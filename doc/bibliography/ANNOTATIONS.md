# Аннотированная библиография: build provenance, SBOM, supply chain security

Метаданные (авторы, год, DOI) — в [papers.bib](papers.bib), ключи записей совпадают.
PDF-копии — в `pdf/` (не в git).

Формат записи — обязательные поля:
- **Статус** — не прочитана / просмотрена / прочитана полностью (+дата)
- **Механизм** — что и как делает
- **Чего НЕТ (vs наш подход)** — позиционирование для статьи
- **Для статьи** — как использовать (related work, контраст, цитата)

---

## Прямые конкуренты: build tracing

### `granat2024-buildography` — Идентификация реквизитов сборки через отслеживание системных вызовов

- **Статус:** прочитана полностью (2026-06-09); текст: `pdf/buildography-paper.pdf`
- **Категория:** ближайший конкурент, обязательна ссылка
- **Механизм:** ptrace + seccomp-BPF (SECCOMP_RET_TRACE только для open/openat/openat2; execve/fork/vfork/clone — через PTRACE_O_TRACE*-флаги). Трассировщик на Си (rhash, uthash; многопоточное отложенное хэширование с заполнителями в буфере), постпроцессор на Python. Хэши ГОСТ Р 34.11-2012 (Стрибог-256). Мотивация — ГОСТ Р 71206-2024.
- **Результаты:** замедление сборки 19–26 % (1 поток), 14–110 % (8 потоков; узкое место — однопоточная обработка ptrace-событий). Замеры: musl, qtbase × gcc, clang.
- **Признанные ограничения (в тексте):** «защита от вредоносных процессов не является целью» — подмена файла посторонним процессом не обнаруживается; ARM64 не имеет SYS_open.
- **Чего НЕТ (vs наш подход):** RPM-атрибуции; git-blob SHA-1 и сверки с `git ls-tree` upstream; идентификации вендоренных зависимостей; SBOM-форматов; CVE; иерархии процессов в выводе (массив component_commands плоский); временных меток.
- **Для статьи:** основная сравниваемая работа. Обзор отвергнутых ими альтернатив (Bear, compiledb, CDE, clang -MJ, FUSE, inotify) можно переиспользовать в related work.
- **Авторы:** студенты/лаборанты ИСП РАН + ВШЭ + СГУ (Гранат — ст. лаборант, Дунаев — аспирант) — не основная компиляторная команда.

### `ispras2024-buildography-manual` — Buildography v1.2.3, руководство

- **Статус:** прочитано полностью (2026-06-09); текст: `pdf/buildography-manual-1.2.3.pdf`
- **Механизм/детали поставки:** CLI `buildography [opts] <prog> [args]`; опции `-d/--dump-dir`, `-f/--filter-subtree`, `-T/--threads`. JSON: `directory`, `commands`, `component_commands[{command, dependencies, output}]`, `input`, `utilities[{path, hash}]`. Имя выходного файла = Стрибог-хэш его содержимого. Анализаторы: `bxy-find-unbuilt-binaries` (ELF/ar, кастомный `--is-binary-pred`), `bxy-find-external-sources`, `bxy_trace_origins` (отчёт plain/PDF через typst). Постпроцессор `bxy_postprocessor` имеет пустые точки расширения get_version/get_config_files_info.
- **Платформы:** AMD64, ARM64; ядро ≥ 4.8 с CONFIG_SECCOMP_FILTER; librhash ≥ 1.3.8, python3, jq. Протестировано: autotools+make, CMake, Meson. Распространяется бинарным tarball в /opt/buildography, лицензия ИСП РАН (закрытый).
- **Статус проекта на июнь 2026:** v1.2.3 (2024-11-20) — последняя публичная; в safec-announce 2025–2026 не упоминается; докладов на OS DAY 2025/2026 и цитирований статьи не найдено. Страница: https://www.ispras.ru/technologies/buildography/ (en-версия — заглушка).

### `ispras2024-sbomtrace` — предшественник: sbom-trace / --dump-sbom

- **Статус:** тред рассылки прочитан (2026-06-09)
- **Механизм:** июнь 2024 — SAFEC получил компиляторный флаг `--dump-sbom <dir>` (журнал по ГОСТ Р 71206-2024) + отдельную ptrace-утилиту sbom-trace (`sbom-tracer -p sbom-postprocessor.py`).
- **Ключевой факт:** Changelog SAFEC 2025 (декабрь 2025): «Удалена опция --dump-sbom» — ИСП РАН отказался от журналирования на уровне компилятора в пользу syscall-трассировки.
- **Для статьи:** аргумент «сами авторы признали недостаточность компиляторного подхода».

### `bomfather2025-ebpf` — Bomfather

- **Статус:** просмотрена (май 2026)
- **Механизм:** eBPF вместо ptrace для захвата файловых операций сборки; Merkle-деревья зависимостей с криптографическими хэшами. Решает: conditional compilation, dead code, dynamic loading.
- **Чего НЕТ (vs наш подход):** RPM-атрибуции, upstream git-верификации, идентификации вендоренных зависимостей.
- **Для статьи:** related work, категория kernel-level monitoring.

### `sbomobserver-buildobserver` — build-observer

- **Статус:** просмотрен (май 2026)
- **Механизм:** наблюдение за файловыми операциями сборки; аналог build-recorder.
- **Чего НЕТ:** RPM-атрибуции, upstream-верификации.

### `omnibor2024-adg` — OmniBOR / GitBOM (arXiv 2024)

- **Статус:** просмотрена по abstract (2026-07-13). Кандидат на полное чтение — ближайший аналог build-recorder.
- **Механизм:** строит Artifact Dependency Graph на gitoid'ах (git-blob SHA как ID артефакта, Input Manifest на каждый выход). ДВА пути сбора: (1) compiler-based — инструментированный компилятор эмитит связи вход→выход; (2) build-tracing — инструмент `bomsh`/`bomtrace` на strace фиксирует gitoid входов/выходов + build command, как build-recorder. Embedding: OID вшивается в артефакт (напр. аннотация @BOM в .class).
- **Чего НЕТ (vs наш подход):** RDF-графа процессов/окружения, RPM-атрибуции, upstream git-верификации. Главное — для managed-компиляции (JVM/.NET) build-tracing-путь бессилен ровно как у нас, и OmniBOR уходит в embedding/compiler-instrumentation, а не восстанавливает рёбра из syscalls.
- **Для нас:** ключевой ориентир и подтверждение диагноза — независимый проект той же архитектуры (strace-трассировка) упирается в ту же стену на in-process компиляции и решает её сменой источника данных. Опора для раздела «почему in-process требует инструментирования, а не наблюдения».

## SBOM / enforcement

### `scored2023-sbom-ebpf-ima` — Enforcing SBOMs through the Linux kernel with eBPF and IMA

- **Статус:** просмотрена (май 2026)
- **Механизм:** kernel-level SBOM enforcement через eBPF + IMA.
- **Для статьи:** про enforcement, не про идентификацию вендоренного кода — разграничить задачи.

### `osv-determineversion` — OSV determineversion API (Google)

- **Статус:** просмотрен (май 2026)
- **Механизм:** принимает {path: sha1}, возвращает ближайший upstream-коммит. Логика аналогична нашему `--upstream-verify`, но: ждёт plain SHA-1 (не git-blob), облачный API, экспериментальный.
- **Для статьи:** ближайший аналог именно upstream-идентификации; подчеркнуть наше отличие — git-blob SHA-1 локально, без облака.

### `classport2025-introspection` — Classport (arXiv 2025)

- **Статус:** просмотрена по abstract (2026-07-13).
- **Механизм:** встраивает информацию о зависимостях прямо в .class-файлы, чтобы извлекать её в рантайме (какие зависимости реально используются при исполнении). Оценка на 6 проектах.
- **Чего НЕТ (vs наш подход):** рантайм-интроспекция потребления зависимостей, не провенанс сборки source→artifact; ничего про то, какой .java произвёл какой .class.
- **Для нас:** образец embedding-подхода к провенансу байткода (родня варианту «метка в артефакте»); показывает, что вшивание метаданных в .class — принятая практика.

### `sbomexe2024` — SBOM.EXE (arXiv 2024)

- **Статус:** просмотрена по abstract (2026-07-13).
- **Механизм:** из SBOM строит allowlist допустимых классов всей цепочки поставки и enforce'ит его при динамической загрузке классов JVM — блокирует неизвестные/подменённые классы (защита от Log4Shell-подобных инъекций).
- **Чего НЕТ (vs наш подход):** enforcement во время исполнения, не построение провенанса сборки.
- **Для нас:** тангенциально — зрелость экосистемы SBOM для JVM (группа Monperrus); разграничить enforcement vs наша задача идентификации/провенанса.

## Supply chain / commit pinning

### `pinning2025-futile` — Pinning Is Futile

- **Статус:** просмотрена (май 2026)
- **Суть:** commit-hash pinning важнее version tags для CI/CD; контекст — атаки на GitHub Actions, не build-time tracing.
- **Для статьи:** мотивационная ссылка на важность точной идентификации коммитов.

### `valsorda2025-survey` — обзор компрометаций supply chain 2024/2025

- **Статус:** просмотрен (май 2026)
- **Для статьи:** мотивация (введение).

### `ishgair2024-sok` — SoK: Defense-Oriented Evaluation of Supply Chain Security

- **Статус:** не прочитана (найдена 2026-06-28)
- **Механизм:** модель AStRA — формализация элементов supply chain и их причинных связей; через неё выводятся цели защиты и оценивается эффективность существующих техник.
- **Для нас:** систематизация для related work — карта, куда ложится наш метод (build-time provenance/attribution) среди защит; источник ссылок и таксономии.

## Смежные работы ИСП РАН

### `dunaev2024-safelang` — Developing a Clang-Based Safe Compiler

- **Статус:** не прочитана
- **Контекст:** та же лаборатория (SAFEC, Buildography); декабрь 2025 анонсирован Safelang.

### `ispras-flagsup` — flagsup

- **Статус:** не изучен
- **Механизм:** экстрактор/суммаризатор флагов сборки (GitHub, открытый — редкость для этой лаборатории).

## Эффективность перехвата системных вызовов

*Цель подборки: снизить накладные расходы build-recorder (у Buildography 14–110 %; у нас сопоставимая проблема — ptrace останавливает процесс на каждом вызове).*

### `yang2024-nexpoline` — Making `syscall` a Privilege not a Right

- **Статус:** не прочитана (найдена 2026-06-09)
- **Механизм:** Nexpoline — перехват syscall через Memory Protection Keys + Syscall User Dispatch: монитор живёт в адресном пространстве процесса, без context switch трассировщика на каждый вызов.
- **Для нас:** кандидат на радикальное снижение overhead vs ptrace; SUD (`prctl(PR_SET_SYSCALL_USER_DISPATCH)`) доступен с ядра 5.11 — реализуемо без eBPF-привилегий.

### `strace2020-seccompbpf` — strace --seccomp-bpf (FOSDEM 2020)

- **Статус:** проход 1–2 сделан (Claude, 2026-08-25) по слайдам (`pdf/strace-seccomp-bpf-fosdem2020.pdf`) и посту автора «Introducing strace --seccomp-bpf» (02.10.2019). Докладчик: Paul Chaignon (в .bib стоял неверный автор, исправлено).
- **Механизм:** cBPF-фильтр возвращает `SECCOMP_RET_TRACE` для номеров syscall, интересных strace, и `SECCOMP_RET_ALLOW` для остальных, поэтому ptrace-останов происходит только на нужных вызовах; в ядре номер сопоставляется двумя алгоритмами (линейный и бинарный поиск по cBPF). Реализовано в рамках GSoC 2018 и 2019 (Chen Jingpiao, Paul Chaignon), доступно с strace v5.3.
- **Цифры:** сборка ядра с трассировкой `connect`: 24m54s без фильтра, 12m48s с `--seccomp-bpf`, 12m27s нативная сборка.
- **Оговорка (цифру нельзя переносить на нас напрямую):** фильтровался один редкий вызов. Наш набор (open/openat/execve/clone) срабатывает на порядки чаще, поэтому двукратное ускорение это верхняя граница выигрыша, а не ожидаемая величина. Наша величина меряется только замером.
- **Ограничения (названы автором):** все потомки трассируются принудительно, как при `-f` (иначе они получают ENOSYS, когда останавливаются без присоединённого трассировщика); не работает при `strace -p <pid>`; нужно ядро 4.8+; фильтровать по аргументам-указателям нельзя из-за TOCTTOU.
- **Для нас:** самая дешёвая оптимизация build-recorder, ровно тот приём, который уже применён в Buildography. Ограничение «все потомки трассируются» нам не мешает: мы и так трассируем всё дерево сборки.

### `aquila2025-telemetry` — Aquila: In-Kernel System Call Telemetry

- **Статус:** проход 1 по абстракту (2026-08-25). Венью закрыт: Sensors 25(21):6511, doi:10.3390/s25216511, октябрь 2025; авторы Juyong Shin, Jisu Kim, Jaehyun Nam (boanlab). Код открыт: github.com/boanlab/Aquila.
- **Механизм:** eBPF-телеметрия системных вызовов целиком в ядре (cloud-native среды). Двухпутевой конвейер разделяет метаданные фиксированного размера и атрибуты переменной длины (дешевле сериализация); приоритетная буферизация и **явное обнаружение потерь событий**; в user space трассы обогащаются метаданными Kubernetes.
- **Для нас:** ориентир, если решим мигрировать с ptrace на eBPF (вместе с `bomfather2025-ebpf`); минус: требует CAP_BPF/root и теряет переносимость на старые ядра. Отдельно ценен приём «явно сообщать о потерянных событиях» (та же проблема, что в `mao2025-provcompleteness`): без него полноту трассы нечем подтвердить.

### `shen2024-aschook` — ASC-Hook: транспарентный syscall-хук для ARM

- **Статус:** не прочитана (найдена 2026-06-28)
- **Механизм:** гибридная замена инструкций + специальный трамплин для перехвата системных вызовов на ARM без ptrace; overhead ~3,7 %, в ~29 раз дешевле существующих инструментов.
- **Для нас:** актуально, только если build-recorder пойдёт на ARM64 (где, как отмечает Buildography, нет SYS_open); на x86_64 наш путь — SUD/seccomp (`yang2024-nexpoline`, `strace2020-seccompbpf`). Держать как ARM-ориентир.

### `wang2018-lprov` — Lprov (ACSAC 2018)

- **Статус:** найдена 2026-07-13, не прочитана.
- **Механизм:** library-aware провенанс: комбинирует kernel audit-log с user-space stack unwinding, восстанавливая происхождение на уровне библиотек внутри процесса, а не только по границам процессов.
- **Для нас (Цель 1):** идея гранулярности «внутри процесса» — направление для снижения overhead и, возможно, частичного заглядывания внутрь долгоживущих процессов. Whole-system forensics, не build-provenance — держать как механизмный ориентир.

### `ma2016-protracer` — ProTracer (NDSS 2016)

- **Статус:** найдена 2026-07-13, не прочитана.
- **Механизм:** практичный провенанс чередованием логирования и тайнтинга по системным событиям — резко снижает объём логов и overhead против сплошного аудита.
- **Для нас (Цель 1):** приём снижения накладных расходов трассировки (у нас ptrace останавливает процесс на каждом вызове). Как и Lprov — форензик хоста, не сборка.

### `grayson2024-provbench` — Benchmark suite и performance-анализ user-space провенанс-коллекторов (ACM REP 2024)

- **Статус:** PDF добыт (`pdf/grayson2024-provbench.pdf`), просмотрен проход 1 (2026-07-27). **Читать самому — прямая опора Цели 1.**
- **Механизм:** методологическая работа, а не инструмент. Собраны 45 system-level провенанс-коллекторов из литературы, классифицированы по способу перехвата, воспроизведены 9 из 15 подходящих; собраны и унифицированы бенчмарки предыдущих работ (у каждой были свои, несравнимые), проведена benchmark minimization → минимальный набор «goalposts» для будущих работ.
- **Ключевые цифры (Table 4, % overhead к walltime).** Компиляция gcc: fsatrace +4, CARE +136, strace +206, RR +321, ReproZip +344. Компиляция Spack: fsatrace −1, CARE +119, strace +111, RR +562, ReproZip +359. LaTeX: fsatrace +7, strace +40. Postmark (I/O-микро): strace +650, ReproZip +1733. Вывод авторов: все коллекторы, кроме fsatrace, слишком дороги для «always-on»; fsatrace дёшев именно потому, что использует **library interposition (LD_PRELOAD)**, а не ptrace. В Future work авторы прямо называют направление: собрать коллектор на library interposition или eBPF вместо ptrace.
- **Чего НЕТ (vs наш подход):** это провенанс вычислительных экспериментов (воспроизводимость науки), а не сборки; нет привязки файлов к пакетам/upstream-коммитам, нет хеширования и графа source→artifact. Категория «Compile user packages» у них — просто нагрузка для замера, а не предмет анализа.
- **Для нас (Цель 1):** (1) готовая система координат для нашего замера overhead — можно сравнивать build-recorder с 9 воспроизведёнными коллекторами на их бенчмарках вместо самодельных цифр; (2) их «Compile w/gcc» и «Compile w/Spack» — ровно наш профиль нагрузки, и strace там даёт +111…206 %, что численно подтверждает наш и Buildography'евский порядок замедления; (3) library interposition как *четвёртое* направление снижения overhead, отсутствующее в GOALS.md (там seccomp-BPF, SUD+MPK, eBPF) — дешевле всех, но ломается на статических бинарях и обходится вызовом syscall напрямую (для враждебной сборки не годится, для честной — годится).

### `mao2025-provcompleteness`: Aegis: полнота провенанса как задача планировщика (arXiv 2025)

- **Статус:** проход 1 по абстракту (Claude, 2026-08-25)
- **Механизм:** формулируется **super producer threat**: генерация провенанса перегружает систему, коллектор роняет события, атакующий прячет свои действия в этой потере, и коллектор перестаёт быть настоящим reference monitor. Изоляция ресурсов проблему не закрывает (упирается в железо и производительность). Предложен Aegis: планировщик Linux на обучении с подкреплением, учитывающий требования полноты провенанса при распределении ресурсов.
- **Чего НЕТ (vs наш подход):** предмет это host-форензика, не сборка; ни артефактов, ни атрибуции.
- **Для нас (Цель 1):** довод, который нам придётся сказать про себя самим. Потеря событий под нагрузкой это не просадка производительности, а дыра в доверии к трассе, и параллельная сборка (`make -jN`) есть ровно super producer. Проверяемое следствие: в отчёте build-recorder нужен счётчик незаписанных/отброшенных событий, иначе утверждение о полноте графа ничем не подкреплено (ср. явную детекцию потерь в `aquila2025-telemetry`).

## Build systems: трассировка файловых обращений ради корректности

*Зачем эта секция (открыта в сессии 7, 2026-08-25): тот же механизм (наблюдать, какие файлы читает сборка) соседнее сообщество эксплуатирует не первое десятилетие, но ради корректности зависимостей, кэширования и герметичности, а не ради провенанса. Для статьи это отдельный абзац related work и заодно защита от рецензента: механизм не нов, нов вопрос, который ему задают.*

### `ren2019-reptrace`: RepTrace: корень невоспроизводимости по syscall-трассе (ASE 2019)

- **Статус:** проход 1 (Claude, 2026-08-25) по abstract, странице конференции и dblp; полный текст за пейволлом IEEE. **Кандидат на чтение самому.**
- **Механизм:** команды сборки трассируются системными вызовами; от разошедшегося артефакта строится граф причинности назад, рёбра двух типов: read/write между процессами и parent/child. Против шума две техники: разностный анализ нескольких сборок сужает пространство read/write-зависимостей, сравнение сходства runtime-значений отбрасывает ложные parent/child.
- **Результаты:** среди Top-10 выданных кандидатов точность 90,00 % по командам и 90,56 % по файлам.
- **Чего НЕТ (vs наш подход):** цель диагностическая (почему две сборки разошлись), а не инвентарная; нет хеширования входов, атрибуции к пакетам и upstream, SBOM на выходе; нужны как минимум две сборки, нам достаточно одной.
- **Для статьи: главная находка сессии 7 по related work.** Структура их графа совпадает с нашей (процессы, рёбра чтения и записи, иерархия родитель-потомок). Значит, наш граф не нов как структура данных, и заявлять новизну надо не в ней, а в вопросе: мы спрашиваем графа об атрибуции входов, они о причине расхождения. Работа сшивает две наши линии (воспроизводимость и трассировка), цитировать в обеих.

### `spall2020-rattle`: Build Scripts with Perfect Dependencies (OOPSLA 2020)

- **Статус:** проход 1 (2026-08-25) по abstract и известной реализации (Rattle, fsatrace).
- **Механизм:** build-скрипт вообще не объявляет зависимости; система выполняет команды и трассирует обращения к ФС через library interposition (fsatrace), получая точное множество прочитанного. Отсюда термин **perfect dependencies**: зависимости не задекларированы, а наблюдены.
- **Чего НЕТ:** провенанса как выхода (трасса потребляется кэшем инкрементальной сборки и наружу не отдаётся), хеш-сверки с upstream, атрибуции к пакетам.
- **Для нас:** (1) готовая терминология: противопоставление «объявленное против наблюдённого» уже названо в рецензируемой работе, и мы бьём декларативные SBOM ровно этим различием; (2) практическое подтверждение, что library interposition тянет настоящие сборки (в `grayson2024-provbench` fsatrace даёт +4 % на компиляции gcc).

### `lyu2024-builddeperrors`: Detecting Build Dependency Errors in Incremental Builds (ISSTA 2024)

- **Статус:** проход 1 по abstract и метаданным (2026-08-25)
- **Механизм:** поиск missing и redundant dependencies в make-скриптах без чистых пересборок (прежний state of the art требовал полных сборок подмножества конфигураций, до нескольких часов на крупных проектах).
- **Для нас:** свежее (2024) свидетельство, что расхождение «объявлено против фактически прочитано» остаётся открытой проблемой и внутри самих build-скриптов, а не только на уровне SBOM. Ссылка для мотивационного абзаца.
- **Смежное (в базу не заводил):** «Detecting Build Dependency Errors by Dynamic Analysis of Build Execution Against Declarations», IEEE TSE 2025, doi:10.1109/TSE.2025.3566225: та же линия, динамический анализ исполнения сборки против объявлений.

### `buildxl-sandboxing`: BuildXL: песочница и наблюдение файловых обращений (Microsoft)

- **Статус:** документация просмотрена (2026-08-25)
- **Механизм:** промышленный build-движок Microsoft. Каждый шаг (pip) исполняется в песочнице с наблюдением файловых обращений: **Detours на Windows, комбинация interpose и ptrace на Linux**. Наблюдённое множество (чтения, пробы отсутствующих путей, перечисления каталогов) входит в ключ кэша; обращение к недекларированному входу это file-access violation, после которого шаг теряет право на кэширование. Песочница умеет и блокировать доступ к чужим путям.
- **Чего НЕТ (vs наш подход):** SBOM, хешей входов и выходов для внешнего потребителя, атрибуции файлов к пакетам и upstream; наблюдение служит кэшу и герметичности, а не отчётности.
- **Для нас:** (1) единственный источник в базе, закрывающий вопрос «а как это устроено вне Linux» (Detours) и показывающий гибрид interpose + ptrace как рабочее промышленное решение; (2) сильный довод, что перехват обращений эксплуатируется в проде годами: новизна не в перехвате, а в том, что делается с графом.

### `mitchell2020-filetracing`: File Access Tracing (Neil Mitchell, 2020)

- **Статус:** прочитан (2026-08-25)
- **Механизм:** разбор четырёх способов узнать, какие файлы читает команда: ptrace, LD_PRELOAD (fsatrace), Detours, FUSE. Для каждого названы слепые зоны: интерпозиция не видит статически слинкованных бинарей и прямых `syscall`-инструкций; ptrace дорог; FUSE меняет семантику ФС.
- **Для нас (Цель 1):** практическая опора под пункт «library interposition», который по итогам сессии 4 надо добавить в GOALS.md, и честный источник для оговорки, почему интерпозиция не годится против недоверенной сборки (обходится в одну инструкцию).

## Идентификация компонентов: source-level

### `woo2021-centris` — CENTRIS (ICSE 2021)

- **Статус:** прочитана полностью (2026-06-09); перевод: [translations/woo2021-centris.ru.md](translations/woo2021-centris.ru.md); PDF: `pdf/woo2021-centris.pdf`
- **Механизм (после прочтения):** гранулярность — функция (ctags + нормализация + TLSH, cutoff 30). P1: сигнатура OSS = функции всех версий без повторов (redundancy elimination, сжатие пространства сравнения в 45 раз: 2,2 млрд → 49 млн функций). P2: сегментация кода — отделение «прикладного кода» OSS от заимствованного через **birth time** функций (дата релиза самой ранней версии, содержащей функцию): если общая функция S и X родилась в X раньше — X вложен в S. Компонент найден, если доля переиспользованного прикладного кода ≥ θ = 0.1. Версия — взвешенное голосование функций по IDF-весу W(f) = log(n/|V(f)|).
- **Результаты:** 91 % precision / 94 % recall (DéjàVu: <10 % / ≤40 %); версия — 91,5 % precision; <1 мин/приложение после препроцессинга. Эмпирика: 95 % переиспользований модифицированы, в среднем берут 48 % кодовой базы с 5 % правок; частичное переиспользование — 97 % случаев.
- **Чего НЕТ (vs наш подход):** не привязан к сборке — статический анализ дерева исходников; не отвечает «какие файлы реально участвовали в компиляции»; идентифицирует версию-релиз, но не произвольный коммит (наш кейс wslay = коммит между релизами им принципиально недоступен: их БД индексирует только теги/версии); точность вероятностная (порог θ), не криптографическая.
- **Для нас (ответы после прочтения):**
  1. Идея «birth time» переносима на git: наш аналог — `git log --follow` по датам появления blob-ов; можно отделять вложенные vendored-deps (их «nested» — ровно наш wslay-в-aria2 кейс).
  2. Приём сегментации применим к --upstream-verify: сравнивать только blob-ы, уникальные для компонента (не встречающиеся в других upstream-репо) — отсечёт ложную атрибуцию общих файлов (LICENSE, configure-скрипты).
  3. Их слабость = наша сила: функции из >10 смешанных версий они признают неидентифицируемыми; git-blob-сравнение с деревом коммитов решает это точно.
  4. Эмпирика «95 % модифицированы, берут 48 % кода» — готовая мотивация, почему наивное сравнение тарболов не работает; цитировать во введении.
- **Код:** https://github.com/wooseunghoon/Centris-public, сервис https://iotcube.net/

### `rasheed2026-hiddendeps` — Hidden Dependencies & Component Variants (arXiv 2026)

- **Статус:** просмотрена по abstract (2026-07-13).
- **Механизм:** статически сканирует .class на ссылки типов (owners вызовов/полей, new-выражения, cast, class-литералы) и маппит внутренние JVM-имена в компоненты SBOM; выявляет скрытые code-level зависимости и варианты компонентов.
- **Чего НЕТ (vs наш подход):** пост-фактум по артефактам, вероятностный маппинг имён; не наблюдает сборку, не даёт source→class рёбер компиляции.
- **Для нас:** прямая иллюстрация «пост-хок анализа метаданных байткода» (родня идее с атрибутом SourceFile/type-refs в .class); показывает и предел подхода — рассогласования и варианты, которых наблюдающая трассировка лишена.

### `dietrich2024-blindspots` — Security Blind Spots of SCA (SCORED '24)

- **Статус:** не прочитана (найдена 2026-08-24 по ссылкам из `darkmatter2026-uncharted`), просмотрен abstract. Закрывает пункт 6 бэклога сессии 5.
- **Механизм:** поиск уязвимых клонов в Maven Central при cloning и shading (код вкопирован в компонент, при shading ещё и перенесён в другое пространство имён), без поддержки собственного индекса. Из 29 CVE с proof-of-vulnerability получено свыше 53 тыс. кандидатов, подтверждено 727 клонов (86 в агрегации по версиям); по результатам обновлены шесть CVE в GitHub Advisory Database.
- **Чего НЕТ (vs наш подход):** только Java/Maven; анализ готовых артефактов постфактум, факт вкопирования устанавливается по сходству кода, а не по наблюдению сборки; какая версия исходника вкопирована, метод не говорит.
- **Для нас:** каноническая ссылка на тезис «вендоринг и shading это слепая зона SCA», то есть мотивация нашего `--upstream-verify` и различения вендоренных копий. Прямой предшественник `rasheed2026-hiddendeps` (те же авторы, Dietrich и Rasheed): сначала показано, что клоны не видны, затем что и SBOM их не выражает.

### `kong2025-ectsbom` — SBOM из дерева внешних компонентов уровня кода (Sci Rep 2025)

- **Статус:** проход 1–2 сделан (Claude, 2026-08-24); PDF: `pdf/kong2025-ectsbom.pdf` (17 стр., open access, PMC12748611). **Самому читать не нужно**: не конкурент, метод статический. Если открывать, то два места: раздел *Benchmarks* (как построен ground truth) и Алгоритмы 1–2 (дерево и обрезка). Код и данные: github.com/xlkong/SBOMdata.
- **Механизм (после чтения):** AST-разбор исходников (Eclipse JDT для Java, модуль `ast` для Python, scala-parser-combinators для Scala), обход всех import-объявлений, затем три фильтра: (1) язык-нативные пакеты (224 базовых пакета, выкачанных из JavaDoc); (2) внутрипроектные (последний слайс это имя класса, оставшийся префикс сопоставляется с путями в дереве проекта); (3) объявлено, но не использовано (имя класса ищется в узлах `SimpleName` вне поддерева import). Два добора к тому, что фильтры не видят: компоненты из конфигурационных файлов (семь предопределённых категорий: драйверы БД, пулы соединений, реализации логирования, Flyway/Liquibase, тест-инструменты, плагины проверки типов, hook-инструменты) и рефлексия через `Class.forName()` (обход `MethodInvocation` + `resolveMethodBinding`, из аргумента берётся строковое полное имя).
- **XCT (в тексте статьи XCT, в аннотации ECT):** дерево из слайсов полного имени, вес листа равен числу объявлений компонента в коде. Обрезка рекурсивная: спуск прекращается на узле с весом больше 1, плюс порог глубины 5 (для Java: два слайса обратного домена, два на проект/регион, пятый на пакет). Издатель, версия, лицензия и описание добираются из онлайн-репозитория Maven; транзитивные зависимости разворачиваются рекурсивно по исходникам самих компонентов.
- **Результаты:** 30 проектов (26 Java, 2 Scala, 2 Python), суммарно 3,45 млн строк, 77 598 import-объявлений, 3354 фактические зависимости. В property-файлах этих проектов 149 раздутых и 18 пропущенных зависимостей. У авторов раздутых нет вовсе, recall 99,8 % (у сравниваемых CycloneDX-Generator, OpenRewrite, Build-Info-Go, Microsoft SBOM-Tool расхождения доходят до сотен: spoon, OpenRewrite, 361 пропущенная). Транзитивные: остальные инструменты дают один шаг, авторы разворачивают дальше (пример alluxio, два шага до `org.objectweb:asm:3.3.1`). Цена: в среднем 194 с на проект против 28–127 с у остальных, neo4j собирается 26 минут.
- **Методологическая оговорка (главное, что даёт чтение):** ground truth построен как **консенсус пяти инструментов, включая их собственный**, с ручным разбором расхождений; авторы сами пишут в threats to internal validity, что при общей системной ошибке инструментов бенчмарк ошибётся вместе с ними. Проверка использования статическая: «dynamic checks for path accessibility were not performed». То есть 99,8 % измерены относительно согласия инструментов, а не относительно факта сборки.
- **Чего НЕТ (vs наш подход):** читают **объявления**, а не входы компиляции. C/C++ прямо назван будущей работой (метод на `#include`, статическую линковку и вендоринг не переносится). Предсобранные бинарники не видны. Условная компиляция и генерация кода на лету не видны. Рефлексия покрыта только `Class.forName()` со строковым литералом. Конфигурационные файлы покрыты списком из семи категорий по ключевым словам, и авторы признают, что на кастомные форматы это не переносится. Нужны исходники не только проекта, но и всех внешних компонентов.
- **Для нас (ответ на вопрос, ради которого открывали):**
  1. Главный вывод: самый аккуратный статический source-level метод всё равно вынужден **угадывать факт использования** (три эвристических фильтра плюс список конфиг-категорий плюс частный случай рефлексии). Трассировка сборки этот факт наблюдает. Это готовая формулировка контраста для введения, притом на их собственных данных.
  2. Их числа полезны как мотивация: в property-файлах 30 тщательно отобранных качественных проектов 149 раздутых и 18 пропущенных зависимостей, а промышленные генераторы расходятся с бенчмарком на сотни компонентов.
  3. **Признание в нашу пользу от лагеря декларативных SBOM:** объясняя, почему Build-Info-Go точнее прочих, авторы пишут, что он «uses building logs to extract dependency, which contains more precise information than static properties». Цитировать.
  4. Их бенчмарк-по-консенсусу это прямая иллюстрация того, что в области **нет ground truth**. Наш трейс на роль ground truth годится; их 30 проектов с опубликованным датасетом это готовый материал для такого эксперимента, если понадобится.
  5. Приём «вес узла плюс порог глубины» применим у нас при свёртке длинных путей в человекочитаемые имена компонентов SBOM-вывода (обратная задача: у нас есть точные пути, нужен читаемый уровень абстракции).
- **Кандидаты на добор из их ссылок:** Xiao et al., JBomAudit (NDSS 2025, doi:10.14722/ndss.2025.240322): сверка официальных SBOM с распространяемыми JAR, широкое несоответствие; Pereira et al.: SBOM по бинарному уровню через zero-shot learning.

## Идентификация компонентов: binary-level (для unbuilt binaries)

*Цель подборки: что делать с бинарником, найденным в сборке, но не собранным из исходников (аналог bxy-find-unbuilt-binaries, но с идентификацией происхождения, а не только обнаружением).*

### `duan2017-osspolice` — OSSPolice (CCS 2017)

- **Статус:** проход 1 сделан (Claude, 2026-06-28); PDF: `pdf/duan2017-osspolice.pdf`. **Корень линии binary-to-source** — читать проход 1, везде пересказан (B2SFinder/B2SMatcher/BinaryAI ссылаются). Авторы: Duan, Bijlani, Xu, Kim, Lee (Georgia Tech), doi:10.1145/3133956.3134048. Открыт на GitHub.
- **Задача (двойная):** в Android-приложениях найти (1) нарушения GPL/AGPL-лицензий и (2) использование уязвимых (1-day) версий OSS. То есть атрибуция бинаря к OSS-проекту **и его версии**.
- **Механизм:** извлекает из бинаря «software birthmarks» и сравнивает с признаками, извлечёнными из **исходников** OSS (binary→source). Признаки **синтаксические**: для нативных C/C++ — строковые литералы + **экспортируемые имена функций** (дёшево, переживают оптимизацию, но не сильную обфускацию); для Java — строковые константы, нормализованные классы, function centroids (обфускация-устойчивые). Ключевая новизна — **иерархическая индексация**: сопоставление по структуре репозитория (строка → файл → каталог), отсюда масштаб и точность одновременно.
- **Масштаб/результаты:** БД 60K C/C++ + 77K Java OSS (миллиарды строк), проанализировано 1,6 млн приложений Google Play. C/C++: recall 82% / precision 87%; Java: recall 89% / precision 92% — лучше BAT и LibScout; по версиям находит на 65% больше, чем LibScout. Если точной версии нет в БД — отдаёт **ближайшую**. Эмпирика: >40K приложений нарушают GPL/AGPL, >100K используют уязвимые версии OSS.
- **Место в линии:** база на **синтаксике** (строки+символы). Её слабость к обфускации/инлайну → мотивация B2SFinder (+ control-flow), а затем BinaryAI (семантические эмбеддинги функций). Знать OSSPolice нужно как «откуда всё пошло».
- **Для нас:** базовый приём «строки + экспортируемые символы» для атрибуции unbuilt-блоба к upstream, когда трассы сборки нет по определению. **Чего НЕТ (vs наш подход):** (1) вероятностное **сходство birthmarks**, не точная провенанс-привязка; при отсутствии версии выдаёт «ближайшую» (мы — точный git-blob или честное «нет»). (2) Работает по **финальному бинарю**, входы сборки не видит — выводит постфактум. (3) Цель — Android-приложения и нарушения лицензий, не дистро-сборка.

### `feng2019-b2sfinder` — B2SFinder (ASE 2019)

- **Статус:** проход 1 сделан (Claude, 2026-06-28, по конференц-странице/конспектам — PDF за пейволлом IEEE/ACM). doi:10.1109/ASE.2019.00100. Второе звено линии после OSSPolice. TODO: полные имена авторов (PDF недоступен открыто).
- **Цель:** переиспользование OSS в **COTS** (закрытом коммерческом) ПО — шире, чем Android-приложения OSSPolice. binary→source, уровень **проекта** (версию ещё не выделяет — это вклад B2SMatcher).
- **Механизм — 7 компиляционно-устойчивых признаков в 3 категориях** (это и есть приращение над OSSPolice):
  - **строковые:** экспортируемые функции, строковые константы, строковые массивы;
  - **числовые:** глобальные массивы констант, глобальные enum-массивы;
  - **control-flow:** паттерны if-else, switch-case jump-таблицы.
  Три метода сопоставления: строковый (инвертированный индекс, точное), числовой (поиск битстрима в `.data`/`.rdata`), control-flow (сравнение семантически эквивалентных паттернов). Два веса значимости: **бинарная информационная энтропия** (для числовых) и **S-IDF** (адаптация TF-IDF).
- **Результаты:** precision 92,3% / recall 88,5%; 21 991 бинарник vs 2 189 OSS-библиотек; находит **до 2,15× больше** случаев переиспользования, чем SOTA (OSSPolice); ~53,85 с/бинарник. Вывод: 63,4% закрытого ПО переиспользует OSS.
- **Что приращено над OSSPolice (нерв линии):** OSSPolice падает, когда строки/символы не совпадают. Кейс Foxit Reader/OpenSSL: общих имён экспортируемых функций нет, строк совпало лишь 19,7% → OSSPolice пропускает. B2SFinder ловит за счёт **control-flow и числовых** признаков + взвешивания. Прогресс линии: синтаксика строк/символов (OSSPolice) → + control-flow + взвешенное сопоставление (B2SFinder) → семантические эмбеддинги (BinaryAI).
- **Для нас:** кандидат на «второй уровень» verify-build — для unbuilt-бинарника устанавливать исходный OSS-проект. **Чего НЕТ (vs наш подход):** (1) по-прежнему **вероятностное сходство** (взвешенное совпадение признаков), не точная провенанс-привязка; (2) уровень проекта, **не версии**; (3) работает по финальному бинарю, входы сборки не наблюдает.

### `b2smatcher2021-version` — B2SMatcher

- **Статус:** проход 2 сделан (Claude, 2026-06-28); PDF: `pdf/b2smatcher2021-version.pdf` (Cybersecurity, SpringerOpen, **открытый доступ**), doi:10.1186/s42400-021-00085-7. Авторы: Gu Ban, Lili Xu, Yang Xiao, Xinhua Li, Zimu Yuan, Wei Huo (Institute of Information Engineering, Chinese Academy of Sciences). **Кульминация линии — прямой конкурент по «второму уровню verify-build», читать самому** (Design §, особенно предсказание скомпилированных файлов и two-stage).
- **Цель:** не просто проект, а **точная ВЕРСИЯ** переиспользованной OSS в COTS-бинарнике. Мотивация — 1-day-уязвимости версионно-специфичны (Heartbleed = OpenSSL 1.0.1–1.0.1g). Берёт кандидатов от OSSPolice/B2SFinder и доводит до версии.
- **Почему прошлых признаков мало:** строки/символы (OSSPolice) и глобальные массивы/control-flow (B2SFinder) **не различают соседние версии** и дают высокие FP при version-identification. Нужны **version-sensitive** признаки.
- **Метод — 5 признаков на двух уровнях + two-stage:**
  - **program-level (2):** строковые литералы, экспортируемые имена функций → этап 1, грубое сопоставление + определение типа переиспользования;
  - **function-level (3):** константы в присваиваниях, константные параметры вызовов, in/out-степени функции на call-graph → этап 2, точное доведение до версии (ловит правки в отдельных функциях между соседними версиями).
- **Два ключевых приёма:**
  1. **ML-предсказание скомпилированных файлов.** Проблема partial build: в бинарь попадает лишь часть исходников (у zlib — **7,3%** файлов), признаки остальных только шумят. Хукать сборку (как Duan 2019) для COTS нельзя — исходники/сборка недоступны. Поэтому **K-means + дерево решений** предсказывают, какие файлы реально компилировались, и признаки берут только из них.
  2. **Абстракция+нормализация функций** — выкинуть дублирующиеся между версиями функции, сравнивать только «уникальные» (масштаб).
- **Новизна — тип переиспользования:** single-version vs **multi-version reuse** (патчинг). Пример: SQLite в одном dll — это 3.6.15, поверх которого добавлены функции из 3.8.8.3. Решение — комбинировать совпавшие признаки И сходство, а не сходство в одиночку.
- **Оценка:** 6351 версия OSS, 585 бинарников (ручная сборка + реальный софт). Precision **89,2%** — выше B2SFinder, +8% к OSSPolice, +7% к коммерческому Cybellum. Нашли уязвимые версии OSS в Zoom, TeamViewer (libcurl 7.65.3 c CVE-2019-5481/5842, zlib 1.2.5, libjpeg-turbo 8b), Tencent.
- **Пределы (Discussion):** инлайн ломает call-graph-степени (деградация function-level); набор скомпилированных файлов зависит от опций (макросы) — при сильно нестандартном конфиге предсказание хуже; признаки выбраны вручную (будущее — семантические, ср. путь BinaryAI).
- **Для нас (сильный контраст):** B2SMatcher **угадывает ML-ом то, что мы наблюдаем точно**. Их центральная трудность — «какие исходные файлы вошли в бинарь» (partial build, 7,3% у zlib) — решается у них вероятностно, потому что сборка COTS вне досягаемости. build-recorder **трассирует фактическую сборку** и знает точный список вошедших файлов как ground truth; они сами называют build-hooking точным путём (Duan 2019), но отвергают из-за недоступности — у нас он доступен по построению. **Чего НЕТ (vs наш подход):** (1) версия как **наиболее вероятная по сходству**, не точный git-blob; multi-version/патчинг — эвристика; (2) ML-предсказание компиляции вместо наблюдения (наш ground truth — их оценка); (3) по финальному бинарю. Это и есть бинарный аналог нашего git-blob-уровня — но вероятностный там, где мы точны.

### `rosenblum2011-toolchain` — Recovering the toolchain provenance of binary code (ISSTA 2011)

- **Статус:** проход 1 сделан (Claude, 2026-06-28, по конф-странице/Semantic Scholar/Vestige — PDF за пейволлом ACM). doi:10.1145/2001420.2001433. Авторы: Nathan E. Rosenblum, Barton P. Miller, Xiaojin Zhu (UW-Madison). **Корень под-ветки тулчейна** («чем собрано», не «что собрано»).
- **Цель:** восстановить **toolchain provenance** бинаря — семейство компилятора (gcc/icc/msvc), а в развитии линии и версию + уровень оптимизации.
- **Механизм:** дизассемблирует IA-32, извлекает **idiom-признаки** (короткие шаблоны инструкций вокруг точек входа функций, FEP) + признаки структуры control-flow; обучает **linear-chain Conditional Random Fields (CRF)**, которые **совместно** моделируют provenance по всему бинарю (используют, что соседний код имеет общее происхождение). Предшественник — их же PLDI'2010 «Extracting compiler provenance» (более простые линейные модели).
- **Результаты:** распознавание семейства компилятора с точностью **~0,925**. Развитие (CFG-признаки + SVM) доводит до полной provenance (семейство + версия + оптимизация).
- **Для нас:** для unbuilt-блоба сообщать не только «чужой», но и «чем собран» — проверка заявлений поставщика (наш `readelf -p .comment` для SAFEC-сборок — тривиальный частный случай). **Чего НЕТ (vs наш подход):** (1) вероятностная **оценка по признакам**, требует дизассемблирования и ручных idiom-фич; (2) выводит компилятор постфактум из бинаря — мы **наблюдаем** фактическую команду компиляции (точная версия gcc + флаги из трассы) как ground truth.

### `binprov2022` — BinProv (RAID 2022)

- **Статус:** проход 1 сделан (Claude, 2026-06-28, по сводкам GMU/конф — PDF за Cloudflare/пейволлом). doi:10.1145/3545948.3545956. Авторы: Xu He, Shu Wang, Yunlong Xing, Pengbin Feng, Haining Wang, Qi Li, Songqing Chen, Kun Sun (GMU и др.). **ML-эндпоинт под-ветки тулчейна** (аналог BinaryAI для линии «чем собрано»).
- **Цель:** идентификация provenance — компилятор + версия + **уровень оптимизации O0/O1/O2/O3**.
- **Механизм:** **BERT**-эмбеддинги прямо по **байтам** бинаря (контекстная семантика/синтаксис), **без дизассемблирования** и без ручного отбора признаков; дообучение классификатора под конкретную задачу provenance. Это и есть главный шаг над Rosenblum (там нужны IA-32-дизасм + рукодельные idioms).
- **Результаты:** точность **92,14% / 99,4% / 99,8%** на уровне байтовой последовательности / функции / бинаря соответственно. Работает и на обфусцированных бинарях; полезно для опознания compiler-helper-функций и улучшения binary similarity.
- **Место в под-ветке:** Rosenblum'11 (дизасм + idioms + CRF) → BinProv'22 (сырые байты + BERT, без дизасма) — та же эволюция «ручные признаки → выученные представления», что и в основной линии (OSSPolice→BinaryAI), но для тулчейна.
- **Для нас:** дешёвый массовый способ узнать «чем собран» каждый blob сборки. **Чего НЕТ (vs наш подход):** (1) вероятностный классификатор по байтам, не наблюдение; (2) даёт компилятор/опции с вероятностью — у нас точная команда из трассы. Тот же контраст «угадывание vs наблюдение», что во всей линии.

### `jiang2024-binaryai` — BinaryAI (ICSE 2024)

- **Статус:** проход 1–2 сделан (Claude, 2026-06-28); перевод-конспект: [translations/jiang2024-binaryai.ru.md](translations/jiang2024-binaryai.ru.md); PDF: `pdf/jiang2024-binaryai.pdf`. Как прямого конкурента — прочитать самому (метод 3.3 locality-driven matching, оценка RQ3).
- **Механизм:** binary-to-source SCA в две фазы. (1) Эмбеддинги функций: модель на базе Pythia-410M, дообучена контрастно (CLIP loss + MoCo), извлекает top-k похожих исходных функций для каждой бинарной (декомпиляция Ghidra, парсинг tree-sitter, корпус 56 млн функций из 12 013 TPL в FAISS). (2) Locality-driven matching: link-time locality (непрерывность функций одного исходного файла в бинарнике, задача покрытия интервалами) + граф вызовов выбирают точную функцию из top-k. TPL-фильтрация через TPLite (function birth time). Порог θ=0,01.
- **Интуиция (что такое «вектор функции»):** эмбеддинг — список из 1024 чисел (выход pythia-410m), точка в 1024-мерном пространстве; кодируется **целая функция**, не инструкции. Обе стороны — псевдо-C от Ghidra и исходник — кодирует одна модель в общее пространство, сходство = косинус между векторами (1,0 — совпали, 0 — не связаны). Пример из статьи (рис. 3): бинарная `FUN_00153d7b` и исходная `DefaultLogDir()` (glog) при разных именах переменных и структуре `if` дают косинус 0,982. Матчинг **семантический**: не по именам (бинарник stripped, имён нет) и не по AST (AST через tree-sitter — только чтобы нарезать исходники на функции, в сопоставлении не участвует). Итоговое «бинарник содержит X» = доля функций X, нашедшихся в бинарнике, > θ.
- **Результаты:** recall@1 модели 22,54 % vs CodeCMR 10,75 %; locality-matching поднимает recall@1 до 66,90 % (потолок 70,45 %). TPL-детект: P 85,84 % / R 64,98 % / F1 73,97 % — обходит академические OSSPolice, B2SFinder и оба коммерческих bsca-A/B. FN в основном (312/366) — частичное переиспользование (доля функций < θ).
- **Методологические оговорки (не брать 85,84 % как реальную метрику):**
  1. **Порог θ=0,01 подобран на тесте** («set θ to 0.01 that achieves the maximum F1 score») — решающий порог тюнингован на том же наборе, где меряют F1, без held-out; пороги бейзлайнов так не оптимизировали (асимметрия). θ=0,01 экстремально низкий (TPL засчитывается при совпадении 1 % функций) — задирает recall, FP-потоп гасится TPLite.
  2. **Асимметрия антиклонового фильтра без абляции.** У BinaryAI есть стадия `FilterByDependency` (TPLite) против функций-клонов, общих между TPL; не сказано, что её дали бейзлайнам. Раздутый FP B2SFinder (1232 > TP 574) скорее всего во многом артефакт отсутствия этого фильтра, а не качества признаков. Нет абляции «BinaryAI без TPLite vs бейзлайны с TPLite» → вклад эмбеддингов/локальности и вклад фильтра в преимущество по точности неразделимы. Бейзлайны вдобавок запущены вне режима (B2SFinder извлекает 1 функцию из корпуса 56 млн — в 5600× больше его проектного масштаба; переписан авторами).
  3. **Генерализация за пределы обучающего тулчейна не показана (не «только Arch»).** Обучающие пары «бинарная↔исходная функция» взяты из ArchLinux/AUR их же пайплайном (makepkg, DWARF) и декомпилированы **Ghidra**; исходный корпус (12 013 TPL) — общий OSS, не Arch-специфичен. И train, и test декомпилированы Ghidra (лок-ин на идиомы конкретного декомпилятора) и скомпилированы авторами фактически gcc-тулчейном Arch (≈-O2, x86_64). Test заявляет «multiple architectures and compiler configurations», но **разбивки по компиляторам/оптимизациям/архитектурам нет** — не показано, что точность держится на MSVC/PE, тяжёлом LTO/-O3, обфускации, не-Ghidra декомпиляции. То есть метрики получены на распределении тулчейна, близком к обучению; для широкого класса «Linux ELF из mainstream-gcc через Ghidra» вероятно обобщается, для чужих компиляторов/декомпиляторов — terra incognita. Вдобавок корпус = вселенная теста (компоненты popular >1K звёзд, гарантированно в базе; длинный хвост недопредставлен), ground truth целиком self-compiled/self-labeled без слепой разметки.
- **Доступность:** proprietary-платформа Tencent (Cloud Security / Keen Lab). В тексте статьи **нет artifact-availability заявления** — не выложены ни веса модели (Pythia-410M + дообучение), ни корпус 56 млн функций / датасет 12 013 TPL, ни обучающий набор (~10 млн пар), ни размеченный SCA-тест (150 бинарников, 1045 компонентов), ни код пайплайна. Публичны лишь хостовый сервис (binaryai.cn/single-file, ≤20 МБ) и API — чёрный ящик. Открыты только зависимости (Ghidra, tree-sitter, Pythia, FAISS), не сборка. `binaryai/CodeCMR` на GitHub — данные бейзлайна-конкурента, не BinaryAI. Коммерческие bsca-A/bsca-B анонимизированы по NDA. **Итог:** заявленные 85,84 % независимо неверифицируемы (нет данных/модели/кода) — для related work отмечать как proprietary, артефакты не опубликованы.
- **Открытые инструменты, используемые в статье** (контраст к закрытости самого BinaryAI — алгоритм собран из открытых кирпичей, но без их корпуса/весов не реплицируем):
  - *Пайплайн:* **Ghidra** (декомпиляция, NSA, Apache 2.0), **tree-sitter** (парсинг исходников, MIT), **Pythia-410m** (базовая LLM, EleutherAI, Apache 2.0; ранее пробовали **OPT**, **BLOOM**), **FAISS** (векторный поиск, Meta, MIT), **makepkg** (сборка обучающих пакетов ArchLinux/AUR, GPL).
  - *Методы/алгоритмы:* **CLIP** (контрастная цель), **MoCo** (расширение негативов), **Hungarian algorithm** (для бейзлайнов).
  - *Форматы/БД:* **DWARF** (адрес→исходник в обучении), **ArchLinux/AUR** (источник пар), **NVD** (уязвимости).
  - *Бейзлайны:* **OSSPolice** ✅ полностью (`github.com/osssanitizer/osspolice`); **TPLite** ✅ (генерирует TPL-dependency; `github.com/Tricker-z/TPLite`, ISSTA'23); **B2SFinder** ⚠️ частично — только ядро без БД/пайплайна (`github.com/1dayto0day/B2SFinder`); **CodeCMR** ⚠️ только данные (`github.com/binaryai/CodeCMR`); **BinPro** ❓ репозиторий не найден (arXiv 1711.00830, U. Toronto).
  - *Закрыто:* bsca-A/bsca-B (анонимизированы по NDA), сам BinaryAI.
- **Контраст для нас:** наш ground truth — точное git-blob-совпадение из трассировки фактической сборки: не зависит от курируемого корпуса (нечего «не найти»), от подобранного порога (совпадение точное, не доля функций) и от качества декомпиляции; не проседает на длинном хвосте. BinaryAI решает обратную, принципиально более шумную задачу (восстановить исходник из stripped-бинарника) — мы наблюдаем входы компилятора напрямую. Формулировка для статьи: **вектор отвечает на вопрос «на что похоже?», git-blob-хеш — «что это ровно такое?»** (косинус 0,98 ≠ тождество: разные функции могут дать 0,95, та же при -O3 — 0,7; у нас blob либо побитово равен upstream-коммиту, либо нет).
- **Чего НЕТ (vs наш подход):** привязки к сборке — это статический анализ готового бинарника, не наблюдение компиляции; не git-blob-точность, а вероятностное совпадение функций; не атрибутирует версию/коммит, только факт переиспользования библиотеки.
- **Для нас:** state-of-the-art «второго уровня» verify-build для unbuilt-бинарников (продолжает OSSPolice→B2SFinder→B2SMatcher); цитировать как сильнейший ML-baseline бинарной идентификации.

### `kim2025-bm25sca` — Cross-language binary-source SCA via BM25 (IJIS 2025)

- **Статус:** проход 1 по абстракту (Claude, 2026-08-25). Полный текст за пейволлом Springer, абстракт скрыт издателем и у агрегаторов (Semantic Scholar отдаёт elided), PDF добыть не удалось: для прохода 2 нужен институциональный доступ. Авторы закрыты: Jong-Wouk Kim, Mi-Jung Choi (Kangwon National Univ.), онлайн 04.11.2025.
- **Механизм:** binary-source SCA для пяти языков (C/C++, Objective-C, Swift, Go) и трёх типов бинарников; вместо дорогих эмбеддингов — BM25-токены, дешевле по вычислениям; recall до 81,4 %. Мотивация авторов, важная для обзора: традиционный SCA требует загрузить исходники на сервер анализа, а вендоры на это не идут (проприетарные алгоритмы, бизнес-логика, данные пользователей), поэтому анализ ведётся со стороны бинаря.
- **Чего НЕТ (vs наш подход):** то же, что у BinaryAI — статический матчинг бинарника без трассировки сборки и без git-blob/коммит-точности.
- **Для нас:** новейшее в линейке binary-to-source; интересен контраст «дешёвый BM25 vs дорогой transformer (BinaryAI)» при сопоставимом recall. Для дуги обзора (ТЕКСТ → СТРУКТУРА → ВЕРСИЯ → СЕМАНТИКА) это обратный ход по цене: после эмбеддингов линия возвращается к лексическому поиску ради вычислительной дешевизны. Отдельный довод в нашу пользу: сама мотивация (владелец кода не отдаёт исходники наружу) отпадает, когда SBOM строится у себя на сборке, а не у стороннего сервиса.

### `zou2025-bincofer` — BinCoFer: three-stage purification для детекта C/C++ TPL в бинаре (JSS 2025)

- **Статус:** не прочитана (найдена 2026-07-27), просмотрен abstract.
- **Механизм:** детект переиспользованных сторонних библиотек **без доступа к исходникам TPL** — признаки извлекаются из бинарной формы библиотеки через binary code similarity detection. Три ступени «очистки» репозитория признаков от избыточности с выделением core functions и признаков уровня функции (работает при частичном переиспользовании библиотеки); вместо порога similarity — взвешенная агрегация схожести функций цели с core-функциями. Сравнение с ModX, B2SFinder, LibAM, BinaryAI на собственном датасете, собранном на **ArchLinux**.
- **Чего НЕТ (vs наш подход):** та же слепая зона всего класса — постериорное вероятностное сходство по готовому бинарю; вход сборки не наблюдается. Плюс отказ от исходников TPL означает, что привязки к коммиту/версии исходника нет вовсе.
- **Для нас:** свежий хвост линии binary-to-source (после `jiang2024-binaryai`, `kim2025-bm25sca`) — уточняет сводную таблицу обзора: ветка «binary-to-**binary**», где источник вообще не участвует. Отдельно полезен датасет на дистрибутивных пакетах ArchLinux (сопоставимо с нашим ALT-материалом).

### `guo2026-bcsdempirical` — Эмпирика BCSD для реальных уязвимостей (arXiv 2026)

- **Статус:** не прочитана (найдена 2026-08-24), просмотрен abstract.
- **Механизм:** не новый алгоритм, а крупная эмпирика binary code similarity detection: 60 тыс. прошивок от 200 вендоров. Выделены четыре фактора, определяющих качество поиска: версия уязвимой функции, область поиска, размер функции и **тулчейн компиляции**. Две предложенные стратегии: build-aware запрос (учитывать, как собрана цель) поднимает MRR с 0,818 до 0,981; двухэтапный TPL-aware поиск с сужением пространства даёт +18,5 %.
- **Чего НЕТ (vs наш подход):** та же слепая зона класса — работа с готовым бинарём без доступа к сборке; «build-aware» у них означает догадку о параметрах сборки, а не запись фактических.
- **Для нас:** числовой довод в пользу главного тезиса подборки. Знание фактической сборки (тулчейн, опции) даёт бинарному матчингу больше прироста, чем смена модели: 0,818 → 0,981 против долей процента на архитектурных улучшениях в соседних работах. Именно эти данные build-recorder выдаёт как ground truth, а `rosenblum2011-toolchain` и `binprov2022` их угадывают. Цитировать в под-ветке тулчейна обзора.

### `pereira2024-zeroshotsbom`: SBOM по бинарю через zero-shot семантическую близость

- **Статус:** проход 1 по абстракту (Claude, 2026-08-25). Препринт arXiv 2403.08799 (03.02.2024, 8 стр.), опубликовано главой: Springer, Signals and Communication Technology, 2025, с. 65–77, doi:10.1007/978-3-031-89994-2_6. Авторы: Devin Pereira, Christopher Molloy, Sudipta Acharya, Steven H. H. Ding (Queen's University).
- **Механизм:** отказ от паттерн-матчинга по строкам: трансформер обучается на семантическую близость «имя продукта ↔ version-строка», встреченная внутри бинаря, и работает zero-shot на продуктах, которых при обучении не видел.
- **Чего НЕТ (vs наш подход):** метод опирается на строку, которую автор бинаря сам о себе написал. У вендоренного, пересобранного или урезанного кода такой строки нет; сборка не наблюдается вовсе, а версия принимается на веру.
- **Для нас:** крайняя точка линии binary-to-source по дешевизне признака: не структура и не CFG, а строки плюс ML-сопоставление. Полезен в обзоре как демонстрация, до какой степени косвенности доходит класс, когда фактическая сборка недоступна. Тот же коллектив (Ding) стоит за `song2024-coejsbundles`.

### `song2024-coejsbundles`: Chain-of-Experts: SBOM для JavaScript-бандлов (arXiv 2024)

- **Статус:** проход 1 по абстракту (Claude, 2026-08-25); arXiv 2408.16198, 29.08.2024.
- **Механизм:** бандл это один оптимизированный файл со срезанными символами и потерянными границами модулей. CoE решает три задачи многозадачной моделью: сегментация кода, классификация, поиск клонов; оценка на 500 веб-бандлах с более чем 66 тыс. зависимостей. Заявлено как первая работа по SBOM для JS-бандлов.
- **Чего НЕТ (vs наш подход):** восстановление постфактум по артефакту, без доступа к сборке; вероятностное сопоставление вместо точного совпадения содержимого входа.
- **Для нас:** JS-бандлинг это тот же вендоринг: входы сборки растворяются в артефакте, и класс работ восстанавливает их догадкой. Расширяет наш тезис за пределы C/C++: проблема не в языке, а в потере информации на сборке. Заодно закрывает вопрос «а что в JS-экосистеме».

## Предсобранные бинарники / phantom dependencies в экосистемах

### `harbuz2026-binarydeps` — Binary Dependencies (FOSDEM 2026)

- **Статус:** просмотрен блог-конспект (2026-06-09)
- **Суть:** «phantom binary dependency» — предсобранный код внутри пакетов (Python wheels через auditwheel — 212 из топ-5000 PyPI), не видимый SCA-инструментам. Обзор: auditwheel, elfdeps, UAPI ELF metadata, PEP 770. Цитирует Xu et al.: 24 % PyPI-проектов транзитивно зовут уязвимые API C-библиотек.
- **Для нас:** терминология и мотивация для статьи; наша трассировка решает эту проблему динамически (видим реальные open() предсобранных .so), без метаданных пакета.

### `pep770-sbom` — PEP 770

- **Статус:** не прочитан
- **Суть:** SBOM-метаданные в Python-пакетах для описания не-Python компонентов.
- **Для нас:** формат-потребитель: наши результаты могли бы заполнять такие SBOM фактическими данными вместо декларативных.

### `larson2025-phantom` — Unmasking Phantom Dependencies (PSF / Alpha-Omega, 2025)

- **Статус:** просмотрен конспект (2026-06-28)
- **Суть:** white paper Сета Ларсона, обоснование PEP 770: phantom-зависимости (Pillow тянет libjpeg/libpng/xz; libwebp с CVE-2023-4863 не виден Syft/OSV-Scanner). SBOM как ecosystem-neutral метаданные.
- **Для нас:** мотивация в пару к `harbuz2026-binarydeps` и `pep770-sbom`; наш динамический трейсинг видит реальные open() этих .so без метаданных пакета.

### `android2024-vendorblobs` — Proprietary Vendor Blobs in Android Firmware

- **Статус:** не прочитана
- **Для нас:** эмпирика масштаба проблемы vendor blobs; methodology обнаружения может пересекаться с нашей задачей unbuilt binaries.

### `linux2024-malware-prevention` — A Study of Malware Prevention in Linux Distributions

- **Статус:** не прочитана
- **Суть:** анализ практик дистрибутивов после XZ/liblzma backdoor.
- **Для нас:** мотивация (введение): XZ-атака прошла именно через артефакты сборки, не видимые в git — ровно наш сценарий обнаружения.

## Качество SBOM-инструментов

### `ares2024-sbom-accuracy` — Accuracy Evaluation of SBOM Tools (ARES 2024)

- **Статус:** не прочитана
- **Суть:** ни один SBOM-генератор не даёт полных и точных SBOM; согласованность инструментов между собой 8–13 %.
- **Для нас:** сильный аргумент для статьи: декларативные SBOM-генераторы (Syft, Trivy) систематически неточны → нужна трассировка фактической сборки.

### `sbom2025-slr` — SLR: SBOM in Supply Chain Security

- **Статус:** не прочитана
- **Для нас:** систематический обзор — источник ссылок и таксономии для related work.

### `ozkan2024-sbomintegrity` — Supply Chain Insecurity: Lack of Integrity Protection in SBOM

- **Статус:** не прочитана (найдена 2026-06-28)
- **Суть:** SBOM-генераторы в популярных языках уязвимы к скрытой подмене инсайдером (содержимое SBOM подделывается незаметно); предложена валидация через публичные репозитории и оценка blockchain-альтернатив.
- **Для нас:** усиливает аргумент против декларативных SBOM (вместе с `ares2024-sbom-accuracy`): мало того что неточны — ещё и не защищены от подмены; трассировка фактической сборки даёт первичное свидетельство, а не редактируемый артефакт.

### `darkmatter2026-uncharted` — Software Dark Matter / DARKFILES (arXiv 2026)

- **Статус:** проход 1–2 сделан (Claude, 2026-07-28) по полному тексту; карта для чтения + конспект: [translations/darkmatter2026-uncharted.ru.md](translations/darkmatter2026-uncharted.ru.md); PDF: `pdf/darkmatter2026-uncharted.pdf`. **Читать самому** (§3.1 threat model, §3.2 definition, §3.4 implementation+limitations, §8) — прямая опора позиционирования. Венью: в PDF незаполненный шаблон ACM («Conference'17», ISBN `978-x-…`) → **не принят/не назначен**, цитировать как arXiv-препринт `2606.13966v1` (11.06.2026, 15 стр.). *(Закрывает TODO из NOTES.md 2026-07-13 про CCS 2026.)*
- **Механизм:** `SDM = Φ(Files_actual \ Files_asserted)` — файлы, физически присутствующие в артефакте, но не заявленные метаданными, после фильтра `Φ` (статическая достижимость от entrypoint'ов + признаки чувствительности: ключи, секреты). DARKFILES: послойное разворачивание артефакта (OCI-слои, вложенные JAR, .hpi/.vsix) → file-to-package индекс (офлайн-БД по 5 Debian + 6 Ubuntu, ~100 ГБ) + regex-правила языковых экосистем → untracked → граф достижимости (shebang/импорты, ELF/PE/Mach-O + RPATH, ссылки в конфигах); `deep-darkfiles` ловит файлы, добавленные в одном слое и стёртые whiteout'ом в следующем. Четыре паттерна происхождения: build-context bleed-in, provenance loss (шейдинг/бандлинг), metadata divergence, post-build drift.
- **Ключевые числа:** DockerHub top-5000 — среднее 30 % untracked, медиана 21 %, взвешенное 37 %, **ни одного образа с нулём**; фильтр достижимости сжимает триаж до ~1 % файлов. Три 0-day: CVE-2025-32754/32755 (`jenkins/ssh-agent`, вшитые SSH host keys, CVSS 9.1 каждый), CVE-2025-32111 (`acme.sh`, GitHub-токен в `.git/config` из промежуточного слоя, CVSS 8.7). Maven: в top-1000 SDM нет вовсе, у отобранных по бандлингу (4889 пакетов) распределение бимодальное, 5098 untracked JAR с CVE. Jenkins: 648 уязвимых JAR в 381 плагине (~200 тыс. установок), 54,6 % невидимы или вводят в заблуждение; +80 через classloader inheritance (копии JAR в архиве плагина нет вообще). OpenVSX: `bundledDependencies` заполняют <2 %. **Packaging Lag:** метаданные догоняют содержимое через несколько релизов, но каждый релиз приносит свежие untracked → сходимости нет.
- **Чего НЕТ (vs наш подход):** вход — готовый артефакт, а не сборка; происхождение файла выводится косвенно (номер слоя, текст инструкции Dockerfile, внутренние VCS), а не наблюдается; нет графа процессов и рёбер source→artifact, нет хешей входов; file2pkg покрывает только Debian/Ubuntu (rpm-мира нет); tracked = «кем-то заявлен», сверки с upstream-исходником не делается; решения авторы сознательно не предлагают («identify, measure, highlight», не comprehensive solution).
- **Для нас:** мощнейшая опора позиционирования. (1) §8 дословно формулирует тезис build-recorder — «tightly coupling transparency metadata generation to the build itself, so that provenance is captured at the point of creation rather than inferred later», и в перечне идущих в эту сторону (SBOMit, in-toto+Witness, Debian `buildinfo`) **трассировки syscall'ов нет** → наше место в их же рамке. (2) Готовый словарь для нашего вердикта: их untracked/SDM ↔ наши GREY/RED, tracked+upstream-verified ↔ GREEN. (3) Три CVE — аргумент «необъяснённый файл эксплуатируем», в мотивацию введения рядом с XZ. (4) Их вывод RQ4 «producer has visibility, consumer is blind — no consumer-side SBOM tooling closes the gap without the producer's build pipeline» — прямое обоснование, зачем инструментировать сборку. (5) Честная оговорка, которую сказать самим: их метод работает на любом чужом артефакте без доступа к сборке, наш — нет; ниши дополняют друг друга. Автор Torres-Arias — из in-toto.

### `bufalino2025-sbomproof` — SBOMproof: Beyond Alleged SBOM Compliance (arXiv 2025)

- **Статус:** не прочитана (найдена 2026-07-27), просмотрен abstract.
- **Механизм:** сравнительное исследование генераторов SBOM и сканеров уязвимостей (open source + облачные сервисы крупных провайдеров) на контейнерных образах, с фокусом на **пакеты ОС в базовых образах Linux**. Результат: инструменты во многом несовместимы между собой, отчёты неточны, часть уязвимостей не детектится вовсе; введён термин «SBOM confusion vulnerability» — расхождение форматов/идентификаторов само по себе становится уязвимостью.
- **Чего НЕТ (vs наш подход):** оценивает готовые SBOM по установленным пакетам; фактическая сборка не наблюдается, файлы внутри пакета не различаются.
- **Для нас:** свежее и более сильное подтверждение тезиса `ares2024-sbom-accuracy`, причём **на дистрибутивных пакетах (rpm/deb)** — то есть ровно на нашем материале (ALT Linux), а не на веб-приложениях. Кандидат в мотивационную часть введения.

### `zhou2026-sbomrealitycheck` — A Reality Check on SBOM-based Vulnerability Management (CODASPY 2026)

- **Статус:** не прочитана (найдена 2026-07-27), просмотрен abstract.
- **Механизм:** эмпирика на 2414 открытых репозиториях. Два вывода: (а) при наличии lock-файла и «крепкого» пакетного менеджера SBOM выходит точным и согласованным — то есть неточность SBOM не универсальна, а зависит от экосистемы; (б) сканеры уязвимостей поверх такого SBOM дают 92,0 % ложных срабатываний, в основном из-за недостижимого кода; анализ вызовов функций снимает 61,9 % ложных тревог.
- **Чего НЕТ (vs наш подход):** экосистемы с lock-файлами (менеджеры пакетов языков), где состав известен декларативно; C/C++-сборка дистрибутива, где lock-файла нет в принципе, не рассматривается.
- **Для нас:** **честный контраргумент**, который нужно учесть, а не замалчивать: там, где есть lock-файл, декларативный SBOM уже точен и трассировка ничего не добавляет. Наша ниша сужается до сборок без манифеста фактических входов (C/C++, дистрибутивные пакеты, вендоринг) — это надо явно сказать в позиционировании, иначе скажет рецензент. Второй вывод (reachability) — аргумент, что от SBOM ждут не только состава, но и достижимости; наш граф сборки к достижимости не ведёт.

### `ali2026-memsbom` — MEM-SBOM: What You See Is Not What You Execute (arXiv, июнь 2026)

- **Статус:** не прочитана (найдена 2026-07-27), просмотрен abstract. Under review, 21 стр.
- **Механизм:** SBOM из **runtime-состояния процесса** методами memory forensics — набор плагинов Volatility 3 для Python: восстановление загруженных модулей из внутренних структур интерпретатора, резолв версий, анализ байткода для графа зависимостей и достижимых уязвимых функций. 51 реальное приложение, 100 % точность извлечения; находит пакеты, пропущенные существующими SBOM-инструментами; без предварительной инструментации.
- **Чего НЕТ (vs наш подход):** момент наблюдения — **выполнение**, а не сборка; нет привязки к исходникам и к тому, из чего артефакт получен; Python-специфично (внутренности CPython).
- **Для нас:** третья точка на оси «когда фиксируем состав»: метаданные (декларативно) → **сборка (мы)** → выполнение (MEM-SBOM). Полезно двояко: (1) заголовок и тезис «то, что видно, — не то, что исполняется» усиливают нашу критику декларативных SBOM; (2) их приём «читать внутренние структуры long-running интерпретатора» — единственный найденный подход, который в принципе заглядывает внутрь процесса-долгожителя, т.е. потенциальный ход для нашей проблемы in-process компиляции (JVM/Roslyn, см. `omnibor2024-adg`), но постериорно и без ptrace. Связать при написании раздела про in-process.

### `xiao2025-jbomaudit`: JBomAudit: аудит официальных Java-SBOM (NDSS 2025)

- **Статус:** проход 1–2 сделан (Claude, 2026-08-25) по полному тексту, PDF: `pdf/jbomaudit2025.pdf`. IBM Research + Indiana University Bloomington.
- **Механизм:** берутся официальные SBOM с Maven Central и соответствующие им JAR. Из байткода класс-файлов (constant pool плюс симуляция стекового исполнения байткода для разрешения вызовов) выводится множество фактически используемых внешних классов, строится дерево зависимостей и сравнивается с задекларированным в SBOM. Расхождения делятся на пропущенные и неверные зависимости.
- **Результаты:** 25 882 SBOM; 7 907 не раскрывают прямые зависимости; 4,97 % пропущенных зависимостей уязвимы. Точность самого инструмента на 30 проектах ground truth: precision 91,74 % и recall 92,30 % на пропущенных, 78,49 % и 81,31 % на неверных.
- **Корневые причины (их раздел VI):** генераторы (CycloneDX Maven и Gradle plugin) строят SBOM по метаданным `pom.xml` / `build.gradle` и не смотрят в код; добавляются ручное включение классов, многомодульность, разнобой плагинов и динамические механизмы.
- **Цитата для статьи:** «The primary SBOM generators in the Java ecosystem (e.g., CycloneDX Maven plugin and CycloneDX Gradle plugin) mainly rely on metadata to generate SBOMs, often neglecting code-level analysis. This can lead to low quality of SBOMs due to erroneous metadata.»
- **Чего НЕТ (vs наш подход):** ground truth у них это артефакт (JAR), а не сборка: видно, что попало внутрь, но не видно, откуда оно взялось и какой ревизии соответствует; динамика (reflection, `Class.forName`) помечается как undetermined; только Java и только байткод.
- **Для статьи:** (1) численно сильнейший на сегодня довод «декларативный SBOM врёт», причём измеренный на официальных SBOM самих разработчиков, а не на сторонних генераторах; пара к `ares2024-sbom-accuracy` (согласованность инструментов 8–13 %) и `zhou2026-sbomrealitycheck`; (2) их рекомендация генераторам это наш тезис, произнесённый с трибуны NDSS; (3) контраст по уровню наблюдения: они восстанавливают использование из байткода готового JAR, мы наблюдаем факт сборки. Вместе с `kong2025-ectsbom` (recall 99,8 % по объявлениям импортов) образует пару «код точнее манифеста», к которой мы добавляем следующую ступень: сборка точнее кода.

## Frameworks: attestation / reproducible builds

### `torres2019-intoto` — in-toto (USENIX Security 2019)

- **Статус:** не прочитана (знаю по контексту)
- **Механизм:** криптографически подписанные attestations каждого шага supply chain; «кто что сделал».
- **Чего НЕТ:** не смотрит ВНУТРЬ шага сборки — шаг доверен, если подписан; происхождение отдельных файлов не верифицируется.
- **Для нас:** комплементарен: наш вывод мог бы стать payload in-toto attestation (предикат для SLSA provenance).

### `lamb2021-reproducible` — Reproducible Builds (IEEE Software)

- **Статус:** не прочитана (знаю по контексту)
- **Суть:** бинарная воспроизводимость как способ верификации сборки.
- **Чего НЕТ:** требует детерминированной сборки (на практике трудно; выкинуто из SLSA 1.0); наша трассировка работает с любой сборкой as-is.
- **Для нас:** ключевой контраст в позиционировании.

### `attestable2025-tee` — Attestable builds (TEE)

- **Статус:** не прочитана
- **Механизм:** верифицируемые бинарники через сборку в trusted execution environment на недоверенных системах.
- **Для нас:** альтернативный (аппаратный) путь к той же цели; контраст: мы не требуем спец-железа.

### `muto2026-attestablechain` — Attestable Build Chain (DSN-W 2026)

- **Статус:** проход 1–2 сделан (Claude, 2026-08-24); PDF: `pdf/muto2026-attestablechain.pdf`. **Механически ближайший сосед нашего подхода во всей секции**: тоже наблюдает фактические файловые обращения сборки, а не сравнивает выходы. Стоит прочитать самому (таблица накладных расходов в §V.D, ограничения в §VI.A, сравнительная таблица C1–C5 в §VI.B).
- **Механизм:** двухфазная аттестация. Фаза 1 подтверждает конфигурацию Confidential VM (AMD SEV-SNP, ядро на COCONUT-SVSM); фаза 2 записывает свидетельства исполнения сборки ядерным Linux IMA (измерения всех открытых файлов и запущенных бинарников), расширяя их в PCR виртуального TPM. Артефакт, подписанный SPDX-SBOM, IMA-лог, PCR quote и SEV-SNP-отчёт упаковываются в Build Chain Report, который третья сторона проверяет **без пересборки**.
- **Модель угроз (полезная формулировка):** T1, подмена тулчейна: воспроизводимая сборка честно воспроизведёт тот же скомпрометированный компилятор и ничего не заметит (Trusting Trust); T2, подмена процесса генерации SBOM. Обе ловятся сверкой хэшей фактически исполненных бинарников и заявленного состава SBOM с IMA-логом.
- **Результаты:** прототип на Bazel, четыре OSS-проекта (guetzli, abseil-cpp, fd, buildifier). Сама CVM почти бесплатна (7,83 → 7,87 с), а IMA-измерение даёт замедление **в 2–5,4 раза**: 7,83 → 39,98; 17,61 → 61,67; 165,32 → 891,87; 160,24 → 313,81 с. Авторы признают, что для итеративной разработки это неприменимо, и позиционируют механизм для релизных сборок.
- **Чего НЕТ (vs наш подход):**
  1. **Нет процессной структуры.** IMA измеряет на уровне ядра всё подряд внутри CVM; авторы сами пишут о шуме от посторонних обращений и выносят добавление uid/pid в будущую работу. Значит нет иерархии процессов и нет рёбер «вход → выход» конкретной команды компиляции: лог отвечает «какие файлы трогали», а не «что из чего собрано».
  2. **Нет атрибуции файлов** (RPM-пакет, git-blob, вендоренная зависимость) и нет различения собранного из исходников и предсобранного бинарника.
  3. **Требует специального железа и окружения:** SEV-SNP, vTPM, кастомное ядро; реализация привязана к Bazel. На обычной сборочной машине дистрибутива (rpm/hasher) неприменимо.
  4. **Стоимость:** 2–5,4 раза против наших десятков процентов и +206 % у strace в `grayson2024-provbench`.
- **Чего нет у нас (честная оговорка для статьи):** их лог непровергаем без доверия к трассировщику, он привязан к аппаратному корню доверия. Мы доверяем собственному ptrace-трассировщику и хосту. Оси ортогональны: наблюдение с атрибуцией (мы) против аппаратно заверенного факта исполнения (они), поэтому комбинация возможна и её стоит назвать самим.
- **Для статьи:** новый прямой контраст в секции «пути к доверенной сборке» и подтверждение диагноза: независимая группа пришла к тому же выводу, что верифицировать надо исполнение сборки, а не эквивалентность выходов. Их таблица C1–C5 (защита окружения, корень доверия, целостность процесса сборки, прозрачность провенанса, проверка без пересборки) годится как готовая рамка сравнения, в которую мы вписываемся строкой «C3+C4 без C1/C2».

### `asad2026-kettle` — Kettle: attested builds (arXiv 2026)

- **Статус:** не прочитана (найдена 2026-08-24), просмотрен abstract.
- **Механизм:** сборка внутри measured confidential VM; в документ провенанса пишутся коммит исходника, набор зависимостей, тулчейн, окружение и дайджесты выходов, а SHA-256 этого документа помещается в поле report_data аппаратного отчёта аттестации. В итоге подписью под провенансом служит сам отчёт TEE, и корень доверия это производитель железа, а не оператор сборочной инфраструктуры. Открытый код (github.com/lunal-dev/kettle), формат SLSA.
- **Чего НЕТ (vs наш подход):** состав провенанса **задекларирован сборочным конвейером, а не наблюдён**: набор зависимостей берётся от менеджера пакетов, фактические открытия файлов компилятором не фиксируются. Это криптографическая подпись под отчётом о сборке, а не свидетельство её исполнения. Ровно этот разрыв и закрывает `muto2026-attestablechain`.
- **Для нас:** третья точка на оси TEE (после `attestable2025-tee` и `muto2026-attestablechain`), удобная для градации в обзоре: подписать заявленное → заверить окружение → измерить исполнение. Наблюдение с атрибуцией входов не покрывает ни одна из трёх.

### `ron2026-zkcompile` — Verifiable Provenance via Zero-Knowledge Compilation (2026)

- **Статус:** проход 1–2 сделан (Claude, 2026-06-28); перевод-конспект: [translations/ron2026-zkcompile.ru.md](translations/ron2026-zkcompile.ru.md); PDF: `pdf/ron2026-zkcompile.pdf`. Прямой соседний путь — прочитать самому (модель угроз §III, оговорка §VIII.B).
- **Механизм:** компилятор исполняется внутри zkVM (арифметизация трассы → полиномиальные ограничения), на выходе — ассемблер + краткое криптодоказательство («receipt»: доказательство + SHA-256 исходника + выход), что компиляция выполнена над заявленным исходником заявленным компилятором. Проверяется кем угодно без пересборки и без доверия железу. 4 свойства контрят 4 угрозы: целостность исходника (дайджест), компилятора (ImageID), выхода, свежесть (anti-replay).
- **Прототип COSMIC TURTLE:** zkVM **RISC Zero** + учебный C-компилятор **ChibiCC** (~10K строк, подмножество C), кросс-компилирован в RISC-V. Открыт: github.com/ASSERT-KTH/verifiable-compilation. Оценка: 200 программ Csmith + 31 файл OpenSSL + 21 libsodium. Все adversarial-тесты (подмена компилятора/исходника/выхода, replay) корректно отклонены.
- **Методологические оговорки (сильно ограничивают «applicable to real-world»):**
  1. **Игрушечный компилятор, подмножество C.** ChibiCC ~10K строк, без inline-asm/GNU-расширений/сложных макросов. Реальные gcc/clang — миллионы строк с оптимизациями; арифметизация их в zkVM на порядки дороже. «Реальные» libsodium/OpenSSL — это **отдельные совместимые .c-файлы**, отобранные вручную, не сами библиотеки.
  2. **Выход — ассемблер, не итоговый бинарник.** Доказывается один шаг исходник→.s одной единицы трансляции. Ассемблирование, **компоновка**, многофайловая сборка пакета (тысячи TU + линковка + build-система) — вне доказательства.
  3. **Overhead ~10 000× + receipt до 120 МБ** на один мелкий файл (медиана zk-компиляции 78,9 с против 0,009 с; проверка ~50 с). Для реального пакета пока непрактично; авторы признают «substantial», списывают на разовость и будущий прогресс zkVM.
  4. **Handshake всё равно требует бутстрап-доверия** к ImageID компилятора (рекурсию «кто собрал компилятор» отложили — самоироничная ссылка [22] на саму статью). Контрится подмена компилятора *после* согласования, но не скомпрометированный *согласованный* компилятор (Thompson trusting-trust).
  5. **zero-knowledge не нужен** (§VIII.B сами авторы): всё публично, zkVM — просто генератор кратких доказательств; по сути это succinct proof-of-execution, а не ZK как таковой.
  6. **Структурный разрыв ISA: x86-64 (боевой) vs RISC-V (доказательный).** zkVM арифметизируют RISC-V (фиксированная длина, ~50 простых инструкций), а не x86-64 — CISC с переменной длиной 1–15 байт, микрокодом и сотнями инструкций арифметизировать на порядки дороже, поэтому нативного x86 zkVM по сути нет (RISC Zero, SP1, Jolt — все RISC-V). Но реальные дистрибутивы (ALT, Debian, Arch) собирают пакеты под **x86-64**. Значит доказывается компиляция *не того* бинарника, что лежит в репозитории (RISC-V вместо x86-64), либо нужен x86-эмулятор поверх RISC-V внутри zkVM — ещё множитель к 10 000× overhead. В прототипе обойдено тем, что выход — ассемблер игрушечного ChibiCC, а не финальный x86-ELF (см. п. 2). Это не деталь реализации, а ограничение подхода — сильный довод для рецензента.
- **Для нас:** свежайший конкурирующий путь к доверенной сборке — пара к `attestable2025-tee` (TEE, аппаратный) и `lamb2021-reproducible`. Явно противопоставляет себя `jiang2024-binaryai` и `duan2017-osspolice` («post-hoc similarity, не гарантия») — здесь они **наш союзник** в аргументе «сходство/пересборка слабы». Но для позиционирования это **конкурент по доверию**: контраст «криптодоказательство одной компиляции игрушечного компилятора при 10 000× overhead vs практическое наблюдение фактической многошаговой сборки реальным gcc с атрибуцией каждого входа (RPM/git-blob)». Они дают криптостойкость на игрушечном масштабе; мы — полную атрибуцию реальной сборки без криптодоказательства нетампертинга (доверяем трассировщику). Оси ортогональны → возможна комбинация.

### `malka2026-lila` — Lila: Decentralized Build Reproducibility Monitoring (2026)

- **Статус:** не прочитана (найдена 2026-06-28)
- **Механизм:** децентрализованная оценка воспроизводимости для функциональной (Nix-подобной) модели пакетов: распределённый сбор результатов пересборки в общую БД, проверка совпадения независимо собранных бинарников.
- **Для нас:** инфраструктурный взгляд на reproducible builds в масштабе; контраст — нам не нужна вторая независимая сборка, мы атрибутируем единственную.

### `malka2025-nixrepro` — Does Functional Package Management Enable Reproducible Builds at Scale? Yes (MSR 2025)

- **Статус:** не прочитана (найдена 2026-07-27), просмотрен abstract. Предшественник `malka2026-lila` (тот же первый автор).
- **Механизм:** пересборка ~710 тыс. пакетов nixpkgs по историческим срезам 2017–2023. Побитовая воспроизводимость 69–91 % с положительным трендом, rebuildability >99 %; ~15 % невоспроизводимости — вшитые в артефакт даты сборки. Опубликован датасет: статусы, логи, diffoscope-разборы расхождений.
- **Чего НЕТ (vs наш подход):** rebuild-and-compare на уровне целого пакета: даёт бинарный вердикт «совпало/нет» и требует полной второй сборки в изолированной функциональной модели; какой файл откуда взялся внутри сборки — не отвечает.
- **Для нас:** **сильнейший на сегодня контраргумент к нашему позиционированию** — тезис «воспроизводимость непрактична, её выкинули из SLSA 1.0» (см. `lamb2021-reproducible`) этой работой прямо оспаривается заголовком «Yes» и цифрами по 710 тыс. пакетов. Формулировку в статье надо смягчить и уточнить: воспроизводимость достижима **при функциональной модели пакетов** (Nix/Guix) с изоляцией и фиксацией входов; для обычного дистрибутива (rpm/hasher) цифры другие — ср. `drexel2025-archverifier`. И главное: даже 100 % воспроизводимость не отвечает на наш вопрос (что попало в артефакт и откуда), а лишь подтверждает детерминированность.

### `choi2026-sbomattest` — Attestation-based verification of SBOM integrity via consumer-side reproducibility (ICT Express 2026)

- **Статус:** не прочитана (найдена 2026-07-27), просмотрен abstract; open access.
- **Механизм:** связка «раскрытие процедуры генерации SBOM + воспроизведение SBOM на стороне потребителя + эталонный дайджест, зафиксированный доверенным центром + сравнение трёх дайджестов (эталон / локально воспроизведённый / доставленный)». Эксперимент на 100 реальных контейнерных образах: детектируется подмена артефакта, подмена SBOM, модификация при дистрибуции и адаптивные подделки, которых не ловит подпись.
- **Чего НЕТ (vs наш подход):** воспроизводится **генерация SBOM по готовому артефакту**, а не сборка; если исходный генератор что-то не увидел (см. `ares2024-sbom-accuracy`, `bufalino2025-sbomproof`), схема добросовестно воспроизведёт ту же неполноту — гарантируется целостность SBOM, а не его соответствие реальности.
- **Для нас:** прямой ответ на проблему из `ozkan2024-sbomintegrity` (подмена SBOM инсайдером) и удобный контраст в позиционировании: целостность SBOM ≠ достоверность SBOM. Наш вывод трассировки — первичное свидетельство сборки, к которому такая схема аттестации может быть надстроена (как и in-toto-предикат).

### `solarin2026-verifiability` — Reproducibility is Not Enough (arXiv 2026)

- **Статус:** не прочитана (найдена 2026-08-24), просмотрен abstract. Самая свежая работа в подборке (препринт от 18.08.2026).
- **Механизм:** четыре экосистемы с децентрализованной сборкой пакетов; собран Artifact Verification Pipeline, воспроизводящий роль независимого верификатора, которому доступны только выводимые из реестра метаданные, с ярусными уровнями эквивалентности артефактов.
- **Результат:** верификация ломается не о недетерминизм сборки, а о нехватку сведений: отсутствующие метаданные источника и сборки, неявные преобразования при подготовке релиза, нестандартные практики сборки. Provenance-аттестации и вшитые VCS-метаданные улучшают картину, но полной спецификации пересборки не дают.
- **Для нас:** сильный довод «воспроизводимость не самодостаточна»: даже при детерминированной сборке независимый верификатор не знает, **что именно** пересобирать. Трейс build-recorder и есть недостающая спецификация входов (фактические файлы, команда, окружение). Хорошая пара к `malka2025-nixrepro`: там показано, что при функциональной модели воспроизводимость достижима, здесь: вне такой модели не хватает самих метаданных. Обе цитаты вместе снимают риск, что рецензент прочтёт наш тезис о воспроизводимости как устаревший.

### `nanni2026-fdroid` — Воспроизводимость сборок в F-Droid (ACM REP '26)

- **Статус:** не прочитана (найдена 2026-08-24), просмотрен abstract. Соавторы пересекаются с `malka2025-nixrepro` и `malka2026-lila` (Malka), плюс Zacchiroli (Software Heritage).
- **Механизм:** исторические логи воспроизводимости F-Droid плюс попытка пересобрать 18 904 версии приложений, ранее подтверждённых как побитово воспроизводимые (09.2018–02.2026).
- **Результаты:** доля побитово воспроизводимых версий растёт со временем; пересобрать удалось 83 %, и среди них 94 % снова дали побитово идентичный артефакт; 76 % провалов пересборки вызваны недоступными зависимостями. Главный вывод: деградирует не воспроизводимость, а сама возможность пересборки (build environment degradation).
- **Чего НЕТ (vs наш подход):** школа rebuild-and-compare целиком, вердикт бинарный, внутрь шага сборки не смотрят.
- **Для нас:** цифра «17 % уже не пересобираются» это довод, что верификация через пересборку имеет срок годности. Запись фактической сборки, сделанная в момент сборки, не протухает и остаётся свидетельством, когда пересборка уже невозможна. Аргумент дополняет `drexel2025-archverifier` (трудность пересборки здесь и сейчас) временным измерением.

### `drexel2025-archverifier` — Independent Verifier for Arch Linux (2025)

- **Статус:** проход 1–2 сделан (Claude, 2026-06-28); подробный конспект: [translations/drexel2025-archverifier.ru.md](translations/drexel2025-archverifier.ru.md); PDF: `pdf/drexel2025-archverifier.pdf`. **Не прямой конкурент — самому читать не обязательно** (школа rebuild-and-compare, не атрибуция); ценно полем «Для нас» (certbot-кейс).
- **Тип:** наполовину обзор-туториал (R-B / bootstrappable builds / Diverse Double-Compiling за 10 лет), наполовину инженерный вклад. Венью — **SICHERHEIT 2024** (GI/LNI, doi:10.18420/sicherheit2024_016), arXiv-препринт 2505.21642 выложен май 2025 (отсюда «2025» в ключе; фактически работа 2024 г.). Авторы — HSLU (Люцерна) + ETH Zürich.
- **Обзорная часть (полезные числа для введения):** причины невоспроизводимости из 422 задокументированных кейсов — таймстемпы 30%, рандомизация 17%, пути 16%. Доли воспроизводимости (ноябрь 2023): Debian 95,2% (Bookworm/amd64), Arch 75,8%. Инструментарий школы: `diffoscope` (бинарный diff в читаемый вид), `rebuilderd` (kpcyrd) — демон, раздающий пересборку воркерам и отдающий статус GOOD/BAD/UNKWN.
- **Вклад (новое в статье):**
  1. Развёрнут независимый верификатор Arch на rebuilderd (reproducible.crypto-lab.ch): sync-демон тянет пакеты каждые 5 мин, воркеры пересобирают и сравнивают побитово. Воркеру нужно ≥32 ГБ RAM на все пакеты, ≥64 ГБ + tmpfs на крупные (ядро, браузеры).
  2. **Certbot — 16 пакетов невоспроизводимы.** Корень: функция `pkgver()` в PKGBUILD меняет описание сборки **после** того, как зависимости уже скачаны и установлены; в SBOM-файл `.BUILDINFO` записывается версия зависимости `python-acme`, не совпадающая с той, что реально требует PKGBUILD. Critical-софт (TLS-сертификаты Let's Encrypt). Предложение: пропатчить тулчейн Arch, чтобы он падал, если функция меняет build-описание после установки зависимостей.
  3. **fwupd — embedded-таймстемп** в заголовке gzip-файла (генерировался Python-stdlib `gzip`, а не GNU `gzip`, потому и проскользнул мимо). Отправили upstream-патч, принят.
- **Для нас (сильный довод, не просто эмпирика):** certbot-кейс — это **ровно наш аргумент «декларативный SBOM врёт, фактическая трассировка — нет»**. `.BUILDINFO` (SBOM) зафиксировал версию `python-acme`, отличную от реально использованной. build-recorder записал бы **фактический** вход компилятора/сборки, а не задекларированную версию — рассинхрон такого рода у нас невозможен по построению. Брать в введение как живой пример провенанс-бага в SBOM. Плюс свежая эмпирика «воспроизводимость на практике трудна» (Arch всего 75,8%) усиливает контраст `lamb2021-reproducible`.
- **Чего НЕТ (vs наш подход):** (1) школа **rebuild-and-compare** — нужна вторая независимая сборка + побитовое сравнение; мы атрибутируем **единственную** фактическую сборку без пересборки. (2) Сборка — чёрный ящик: сравниваются выходы (diffoscope), внутрь шага (какие именно байты/файлы вошли) не смотрят — это и есть наша ниша syscall-трассировки. (3) Атрибуции «этот блоб ← это git-дерево» нет; невоспроизводимость показывает «расходится», но не «чей вход виноват» без ручного diff.

### `dietrich2026-mavenrebuild` — Variability of Source Code in Maven Rebuilds (2026)

- **Статус:** не прочитана (найдена 2026-06-28)
- **Суть:** совпадают ли исходники при независимой пересборке Java-пакетов Maven Central (Google Assured OSS, Oracle Build-from-Source). Главная причина расхождений — build-extensions, генерирующие код во время сборки.
- **Для нас:** довод, что «тот же исходник» нетривиально даже до бинарной воспроизводимости; сгенерированный на лету код наш трейсинг фиксирует фактически (реальные входы компилятора), а декларативный SBOM его пропускает.

### `sharma2025-unreproducible` — Causes of Unreproducible Java Builds (IEEE TSE 2025)

- **Статус:** просмотрена по abstract (2026-07-13).
- **Механизм:** первая крупная таксономия — 6 корневых причин невоспроизводимости Java-сборок (манифесты, SBOM, ФС, байткод JVM, версии, таймстемпы); канонизаторы байткода jNorm и OSS-Rebuild как митигация.
- **Чего НЕТ (vs наш подход):** про воспроизводимость постфактум (сравнение пересборок), не про наблюдение исходной сборки.
- **Для нас:** Java-специфика для позиционирования vs reproducible builds; перечень источников недетерминизма пригодится, если будем трассировать JVM-сборки (что фиксировать/канонизировать).

## Стандарты

### `gost71206-2024` — ГОСТ Р 71206-2024 (безопасный компилятор C/C++)

- **Суть:** требует журналирования трансляции: рабочий каталог, имена и хэши единицы трансляции/выходного файла/всех включаемых файлов, полная команда компиляции.
- **Для статьи:** нормативная мотивация всей предметной области в РФ.

## Патенты

### `patent2024-12164911` — Monitoring and auditing secure software builds (IBM)

- **Статус:** прочитан по Google Patents (2026-06-09)
- **Механизм:** НЕ syscall-трассировка как таковая, а адаптивный monitoring level per build instruction: indicator value → «глубина trace activities» → изменение детализации логирования; hardcoded mapping table (claim 9). Trace activities: сеть, сообщения, данные процессов, подпрограммы, CLI-ввод. Анализ: permitted/unpermitted списки инструкций; enforcement mode (убить сборку) или warning.
- **Патентный риск:** нет. Все независимые claims (1, 11, 12) требуют indicator value + изменение уровня логирования; у нас единообразная полная запись без уровней.
- **Чего НЕТ:** хэширования содержимого файлов, атрибуции происхождения.
- **Для статьи:** пример industrial policy-based build monitoring (IBM решает «много логов → детект», мы — «чьи это байты»); контраст выборочного логирования с полной записью.

### `patent2010-7721272` — Tracking file access patterns during a software build

- **Статус:** просмотрен (май 2026)
- **Механизм:** ptrace + file access → dependency graph для инкрементальных сборок. Цель — скорость сборки, не безопасность.

### `patent2021-11144438` — Incremental build system using an inferred dependency graph

- **Статус:** просмотрен (май 2026)
- **Механизм:** аналогичный патент, system-monitor подход.

### `patent2024-12067122` — Detecting added functionality in open source package (Microsoft)

- **Статус:** прочитан полностью, включая все 15 claims (2026-06-09, OCR скана); перевод формулы и анализ: [translations/patent2024-12067122.ru.md](translations/patent2024-12067122.ru.md); PDF: `pdf/patent2024-12067122.pdf`
- **Реквизиты:** Microsoft Technology Licensing (Shaver, De Castro, Shanmugam, Mazor); подача 2020-12-22, выдача 2024-08-20.
- **Механизм:** «пересобери и сравни» (приватизированная вариация reproducible builds): найти репозиторий исходников → пересобрать пакет в hardened-среде с параметрами исходной сборки → сравнить с опубликованным пакетом (побитово без подписей, затем по файловой структуре) → alert при расхождении. Независимые claims: 1 (система, интерпретируемый код), 7 (способ), 15 (система, машинный код + извлечение метаданных компилятора).
- **Патентный риск:** **нулевой** — все независимые claims требуют пересборки пакета и функционального сравнения пересобранного с исходным; в нашем методе этих шагов нет (наблюдаем единственную реальную сборку, атрибутируем входы хэшами).
- **Чего НЕТ (vs наш подход):** атрибуции происхождения (метод бинарный «совпало/нет», не скажет, откуда расходящийся файл); устойчивости к недетерминизму сборки (их «оценка согласованности различий» — эвристика); сравнение файловых структур обходится вредоносом с теми же именами/размерами.
- **Их сильная сторона:** применим post-hoc к уже опубликованному пакету; наша трассировка требует присутствия при сборке. Подходы комплементарны.
- **Для статьи:** контраст «rebuild & compare (требует воспроизводимости — исключена из SLSA 1.0) vs наблюдение фактической сборки». В их prior art цитируется OSSPolice (`duan2017-osspolice`).

### `patent2019-10296326` — Identifying open-source software package based on binary files

- **Статус:** проход 1 по claims (Claude, 2026-08-25), Google Patents US10296326B2. Закрыт TODO по assignee.
- **Реквизиты:** Insignary Inc (продукт Clarity), изобретатели Younggon Kim, Tae-Jin Kang, Si Haeng Cho; подача 2018-01-31, выдача 2019-05-21; 24 claims.
- **Механизм (claim 1):** заранее строится БД эталонных бинарников, собранных из известных OSS-пакетов под разные CPU и ОС, с двумя видами хешей: обычными (точное совпадение фрагментов) и similarity-хешем. Для целевого бинарника извлекаются хеши и ищется совпадение **минимум двух** обычных хешей; если их нет, метод откатывается на similarity-хеш, выбранный по имени файла.
- **Чего НЕТ (vs наш подход):** сборка не наблюдается; определяется пакет, а не версия и тем более не коммит; необходимость собрать эталоны под каждую комбинацию тулчейна и платформы и есть цена метода (та же слабость, что у всей линии binary-to-source).
- **Для нас:** раннее (2018) промышленное воплощение линии binary-to-source на хешах, до ML-эпохи: удобная стартовая точка дуги ТЕКСТ → СТРУКТУРА → ВЕРСИЯ → СЕМАНТИКА в обзоре. Отдельная деталь для контраста: у них порог «два совпадения хешей», у нас порога нет вообще (git-blob либо совпадает с upstream, либо нет).

### `patent2017-9535687` — Audited builds based upon separate class dependency records (IBM)

- **Статус:** проход 1 по claims (Claude, 2026-08-25), Google Patents US9535687B2. Закрыт пункт 5 бэклога.
- **Реквизиты:** IBM, изобретатели Geoffrey M. Clemm, Bryan P. Douros, Steven T. Rehrauer; подача 2005-05-27, выдача 2017-01-03.
- **Механизм (claim 1):** во время **audited build** логика сборки эвристически определяет зависимости каждого класса и пишет их **в отдельную конфигурационную запись на каждую цель makefile**, а не в один общий файл; решение о пересборке принимается по версиям записей, а не по временным меткам.
- **Чего НЕТ (vs наш подход):** цель это корректная инкрементальная сборка Java, а не отчёт о происхождении; ни хешей, ни атрибуции к пакетам и upstream, ни SBOM; зависимости определяются эвристиками сборки, а не наблюдением ядра.
- **Для нас, два вывода:** (1) prior art на «сборка, записывающая свои зависимости» датируется 2005 годом: новизна нашей работы не в самом факте записи, и заявлять её так нельзя; (2) для Java зависимости берутся из логики сборки, а не из системных вызовов, то есть это ровно та сторона нашего гибрида «метаданные дают рёбра, трассировка верифицирует узлы и хеши». Подтверждает вывод сессии 3: для in-process компиляции путь наблюдения обходят инструментированием, и делают это давно.

### `patent2025-12229279` — CBOM generation from binary executables (Binarly)

- **Статус:** просмотрен по Google Patents (2026-06-28)
- **Реквизиты:** Binarly Inc (Matrosov, Thomas, Vasilenko); подача 2024-10-25, выдача 2025-02-18 (B1).
- **Механизм:** анализ бинарников через промежуточное представление + ML-модели для выявления криптофункций и их параметров; генерация Cryptography BoM (CBOM) без исходников.
- **Чего НЕТ (vs наш подход):** узкая ниша — только криптоассеты, не общий граф сборки; статический анализ готового бинарника, не трассировка; не атрибутирует происхождение/версию компонентов.
- **Для статьи:** пример специализированного BoM «из бинарника» — соседнее патентное поле; пересечения с нашей syscall-трассировкой нет.

---

### `patent2025-12333020` — SBOM for large distributed builds (Microsoft)

- **Статус:** просмотрен по abstract (2026-07-13).
- **Механизм:** генерация SBOM для больших распределённых сборок: отбрасывает из компонентных SBOM файлы, не использованные в финальной сборке, и добавляет инкорпорированные, но неатрибутированные фрагменты; конфиг-ограничения снижают false-positive уязвимостей.
- **Чего НЕТ (vs наш подход):** патентная формулировка результата, не наблюдающая трассировка ядра; ничего про RDF-граф/upstream-верификацию.
- **Для нас:** патентное поле build-time SBOM от Microsoft; «adding declarations for code incorporated but not properly attributed» перекликается с нашей задачей атрибуции phantom/unbuilt-фрагментов.

### `patent2026-12549587`: Correlating outbound traffic and file events with a CI/CD pipeline (Step Security)

- **Статус:** прочитаны claims и релевантные части описания (Claude, 2026-08-25) по Google Patents US12549587B2.
- **Реквизиты:** Step Security Inc (продукт Harden-Runner), изобретатели Varun Sharma, Ashish Kurmi; подача 2024-01-27, выдача **2026-02-10**; 15 claims.
- **Механизм:** сенсоры в раннере CI/CD (агент, DNS-прокси и Linux Audit; для self-hosted Kubernetes eBPF) отдают runtime-события. Сервер корреляции по временам начала и конца шага сопоставляет события с шагом конвейера и получает: базовую линию разрешённых исходящих эндпоинтов с блокировкой отклонений, обнаружение перезаписи исходников и артефактов сборки, и **SBOM по компонентам, выведенным из runtime-событий** (claim 1 и claim 3).
- **Цитаты для статьи.** Из описания: current SBOM solutions «rely on software dependency manifest and lock files in source code ... or analyze the software artifacts once it has been produced ... Both solutions fail to meet industry needs since they provide incomplete information». И далее: «the system can generate SBOM using runtime events instead of by just analyzing dependency manifest files or the generated software build».
- **Чего НЕТ (vs наш подход):** гранулярность это **шаг конвейера**, а не процесс и не команда компиляции; сопоставление идёт по временным окнам шага, а не по иерархии процессов из ptrace; компоненты **выводятся** (inferred), преимущественно из сетевых загрузок, то есть это инвентарь скачанного, а не входов компилятора; нет хеширования входов, нет сверки git-blob с upstream, нет атрибуции файла к пакету, нет рёбер «вход → выход» команды; сам SBOM описан одной строкой claim'а, без метода.
- **Для нас: ключевая находка сессии 7 по патентному полю.** Тезис «SBOM надо строить по наблюдённым runtime-событиям сборки, а не по манифестам и не по готовому артефакту» с февраля 2026 закрыт патентом США. Следствия: (а) патент обязателен в статье как ближайшее патентное prior art, иначе его найдёт рецензент; (б) нашу новизну надо формулировать там, где патент пуст: атрибуция входов (пакет, upstream-коммит, git-blob), процессная иерархия вместо временных окон, рёбра «вход → выход» конкретной команды компиляции, хеши как предмет проверки; (в) методический урок: в сессии 6 этот номер был отбракован по названию («про исходящий трафик»), и отбраковка оказалась неверной. Патенты отсеивать по claims, а не по заголовку.

## Наш уникальный вклад (чего нет ни в одной из перечисленных работ)

- Прямое сравнение git-blob SHA-1 из ptrace-трассировки с `git ls-tree` upstream
- RPM-атрибуция каждого файла с фиксом path-alias (lib64 ↔ usr/lib64)
- Разделение static/dynamic deps через классификацию процессов (compiler vs linker)
- Практический результат: wslay в aria2 = коммит 2022-08-25, а не release-1.1.1 (как утверждает configure.ac)
